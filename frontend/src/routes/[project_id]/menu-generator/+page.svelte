<script lang="ts">
  /**
   * Pagina Menu Generator (scoped a proyecto)
   *
   * URL: /[project_id]/menu-generator
   *
   * El contexto lo aporta el módulo pizzepos/menu-generator en el backend,
   * resuelto desde la ruta por prompt-engine.
   */
  import { LazyShell } from '$lib/components/layout';
</script>

<LazyShell />
