{
  "name": "tarifas",
  "version": "3.1.1",
  "_v3_1_1_changelog": "v3.1.1 (2026-06-01): aplica TP4 del contrato subsistema-carta v1.0.0. Declaracion explicita en events.publishes.tarifas.config.actualizada.description de que correlation_id es OBLIGATORIO en el payload, alineado con P9/D10 del contrato (correlation_id obligatorio en los 5 eventos canonicos del subsistema). Cero cambio en runtime --el helper centralizado de publishes (index.js lineas ~76-91) ya construye correlation_id en todos los publishes desde antes. Bump patch porque el comportamiento no cambia; solo se afina la documentacion.",
  "description": "Asignacion carta+canal + registro de variantes pizzepos. Cada canal tiene su carta con precios finales escritos. Sin calculos en runtime, sin duplicacion (tarifas-creator y tarifas-sync agentes lo hacen). Publica tarifas.config.actualizada con el snapshot completo del proyecto en cada cambio; suscribe tarifas.config.solicitada para que otros modulos hidraten su cache sin acoplamiento directo.",
  "author": "Pizzepos Team",
  "language": "es",
  "main": "index.js",
  "events": {
    "publishes": [
      {
        "event": "tarifas.config.actualizada",
        "description": "Config cambiada o snapshot del estado actual. Payload: project_id (obligatorio), correlation_id (OBLIGATORIO --canonizado por contrato subsistema-carta v1.0.0 P9/D10), timestamp (obligatorio), tipo (obligatorio enum: general|assign|variant_registered|snapshot), config (obligatorio, estado completo: general, canales, variantes). Permite a otros modulos mantener cache local sin acceso directo."
      }
    ],
    "subscribes": [
      {
        "event": "project.activated",
        "handler": "onProjectActivated",
        "description": "Cache base_path + carga config del proyecto."
      },
      {
        "event": "project.deactivated",
        "handler": "onProjectDeactivated",
        "description": "No-op multi-tenant: preserva config en memoria."
      },
      {
        "event": "tarifas.config.solicitada",
        "handler": "onConfigSolicitada",
        "description": "Otro modulo pide snapshot del estado actual. Re-publica tarifas.config.actualizada con tipo=snapshot para cada proyecto conocido (o uno especifico si trae project_id)."
      }
    ]
  },
  "ui_handlers": [
    {
      "domain": "tarifas",
      "action": "get",
      "handler": "handleGet",
      "type": "workspace_module",
      "zone": "barra_modulos"
    },
    {
      "domain": "tarifas",
      "action": "assign",
      "handler": "toolAssign",
      "type": "workspace_module",
      "zone": "barra_modulos"
    },
    {
      "domain": "tarifas",
      "action": "health",
      "handler": "handleHealth",
      "type": "workspace_module",
      "zone": "barra_modulos"
    }
  ],
  "tools": [
    {
      "name": "tarifas.set_general",
      "description": "Establece la carta general (la que usan los canales sin carta propia)",
      "handler": "toolSetGeneral",
      "parameters": {
        "type": "object",
        "properties": {
          "carta_id": {
            "type": "string",
            "description": "ID de la carta general"
          },
          "project_id": {
            "type": "string",
            "description": "ID del proyecto (default: ultimo activo)"
          }
        },
        "required": [
          "carta_id"
        ]
      },
      "errores_conocidos": [
        "INVALID_INPUT",
        "UNKNOWN_ERROR"
      ]
    },
    {
      "name": "tarifas.assign",
      "description": "Asigna una carta a un canal. Si carta_id es null, el canal vuelve a usar la general. Canales: mesa, llevar, telefono, whatsapp, glovo, llevadoo.",
      "handler": "toolAssign",
      "parameters": {
        "type": "object",
        "properties": {
          "canal": {
            "type": "string",
            "enum": [
              "mesa",
              "llevar",
              "telefono",
              "whatsapp",
              "glovo",
              "llevadoo"
            ],
            "description": "Canal de venta"
          },
          "carta_id": {
            "type": "string",
            "description": "ID de carta a asignar (null = usar general)"
          },
          "project_id": {
            "type": "string",
            "description": "ID del proyecto (default: ultimo activo)"
          }
        },
        "required": [
          "canal"
        ]
      },
      "errores_conocidos": [
        "INVALID_INPUT",
        "UNKNOWN_ERROR"
      ]
    },
    {
      "name": "tarifas.get",
      "description": "Config actual: carta general + que carta usa cada canal + variantes registradas",
      "handler": "toolGet",
      "parameters": {
        "type": "object",
        "properties": {
          "project_id": {
            "type": "string",
            "description": "ID del proyecto (default: ultimo activo)"
          }
        }
      },
      "errores_conocidos": []
    },
    {
      "name": "tarifas.register_variant",
      "description": "Registra una variante de carta con sus reglas y la asigna a canales. Las reglas describen como se creo (para que tarifas-sync pueda reproducir los cambios cuando la base cambie).",
      "handler": "toolRegisterVariant",
      "parameters": {
        "type": "object",
        "properties": {
          "carta_id": {
            "type": "string",
            "description": "ID de la carta variante"
          },
          "base_carta_id": {
            "type": "string",
            "description": "ID de la carta base de la que se derivo"
          },
          "nombre": {
            "type": "string",
            "description": "Nombre descriptivo de la variante"
          },
          "canales": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Canales que usan esta variante"
          },
          "reglas": {
            "type": "object",
            "description": "Reglas de la variante en lenguaje natural."
          },
          "project_id": {
            "type": "string",
            "description": "ID del proyecto (default: ultimo activo)"
          }
        },
        "required": [
          "carta_id",
          "base_carta_id"
        ]
      },
      "errores_conocidos": [
        "INVALID_INPUT",
        "UNKNOWN_ERROR"
      ]
    },
    {
      "name": "tarifas.get_variants",
      "description": "Lista las variantes registradas con sus reglas y canales asignados",
      "handler": "toolGetVariants",
      "parameters": {
        "type": "object",
        "properties": {
          "project_id": {
            "type": "string",
            "description": "ID del proyecto (default: ultimo activo)"
          }
        }
      },
      "errores_conocidos": []
    }
  ],
  "dependencies": [],
  "tracing": {
    "propaga_correlation_id": true
  },
  "config": {
    "persistence": {
      "type": "json-file-per-project",
      "owner": "TarifasModule (Map configPerProject + Map projectPaths)",
      "paths": [
        "{project.base_path}/storage/config/tarifas.json (write atomico tmp+rename)"
      ],
      "restart_resilient": true,
      "concurrency": "single-writer-per-project",
      "lost_on_restart": []
    }
  },
  "observability": {
    "logging": {
      "level": "info",
      "structured": true,
      "correlation_id": true
    },
    "metrics": {
      "enabled": true,
      "counters": [
        "tarifas.assign.updated",
        "tarifas.variant.registered",
        "tarifas.errors"
      ]
    }
  }
}
