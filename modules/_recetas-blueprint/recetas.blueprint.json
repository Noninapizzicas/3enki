{
  "_doc": "Blueprint CONCRETO del modulo recetas v1.0.0. Reemplaza completamente al modulo recetas legacy v4.0.0 (cuyo index.js queda como referencia historica). Declara las 13 operaciones del dominio en formato declarativo: contrato + pseudocodigo + reglas. El LLM ES el runtime: lee este blueprint (junto con el padre) como su contrato, sigue el pseudocodigo de cada operacion publicando eventos al bus directamente via bus.publish / bus.publishAndWait. NO existe runtime intermedio. NO existen helpers JS del dominio.",

  "id": "recetas",
  "version": "blueprint-1.0.0",
  "extends_blueprint_abstract": "subsistema-recetario.modulo-base",
  "language": "es",

  "rol": "Aggregate root del subsistema-recetario. Custodio del dato canonico de cada receta y del catalogo de ingredientes del proyecto.",

  "garantiza": [
    "Cada receta tiene id (uuid), nombre, version (entero >=1), estado_operativo in {borrador|en_servicio|archivada}.",
    "No hay duplicados activos: dos recetas no pueden tener el mismo nombre normalizado (lowercase + trim) en estado_operativo=en_servicio simultaneamente.",
    "Persistencia atomica en /recetas.json (path relativo al base_path del proyecto, resuelto por filesystem internamente cuando se incluye project_id en el payload).",
    "Cada operacion de mutacion publica el evento canonico correspondiente (receta.creada, receta.actualizada, receta.estado.actualizada, receta.eliminada, ingrediente.precio.actualizado).",
    "Cada actualizacion pushea snapshot al history[] de la receta y bumps version (+1). Las versiones anteriores se pueden restaurar via revertir.",
    "Las transiciones de estado_operativo respetan TRANSICIONES_VALIDAS (declaradas en el padre). Transicion invalida → PRECONDITION_FAILED.",
    "incompleta + campos_pendientes se recalculan en cada mutacion (crear, actualizar, revertir) — campos_pendientes son los de {nombre, ingredientes, instrucciones, porciones} vacios."
  ],

  "no_garantiza": [
    "Coste real — escandallo escucha receta.creada/actualizada y calcula via su propio pseudocodigo.",
    "Viabilidad economica — viabilidad calcula tras escandallo.",
    "Sugerencias culinarias creativas — recetario-creativo (modulo aparte) propone prototipos.",
    "Catalogo de ingredientes detallado (alergenos, origen) — solo precio_mercado + categoria + unidad."
  ],

  "convencion_de_paths_filesystem": {
    "_descripcion": "filesystem espera paths LEADING-SLASH relativos al base_path del proyecto. NO construir paths absolutos del sistema concatenando base_path — duplica el prefijo. El project_id viaja en el payload y filesystem resuelve internamente.",
    "correcto":   "publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
    "incorrecto": "publishAndWait('fs.read.request', { path: base_path + '/recetas.json' })  // duplica prefijo"
  },

  "estado_persistente": {
    "_descripcion": "El unico estado del modulo vive en disco como JSON en /recetas.json del proyecto. Estructura del store.",
    "ubicacion_logica": "/recetas.json (relativo al base_path del proyecto)",
    "shape": {
      "_version":              "string ('1.0')",
      "_updated_at":           "ISO 8601 — se refresca en cada mutacion",
      "recetas":               "array de Receta",
      "ingredientes_catalogo": "array de IngredienteCatalogado"
    },
    "shape_receta": {
      "id":                "uuid v4 (generado por el LLM cuando crea — en esta version vale uuid alucinado del LLM; sugerencia futura: primitiva bus.newId)",
      "nombre":            "string trim",
      "descripcion":       "string | ''",
      "ingredientes":      "array de { nombre, cantidad: number|null, unidad: string|null, notas: string }",
      "instrucciones":     "array de string (pasos numerados)",
      "porciones":         "number > 0 | null",
      "tiempo_min":        "number >= 0 | null",
      "dificultad":        "number 1-10 | null",
      "categorias":        "array de string",
      "etiquetas":         "array de string",
      "fuente":            "string in {manual, importada, sugerida, agente} — default 'manual'",
      "notas":             "string | ''",
      "estado_operativo":  "borrador | en_servicio | archivada",
      "version":           "number (1 al crear, +1 por update)",
      "history":           "array de snapshots de versiones anteriores ({ ...receta sin history, _archived_at: ISO })",
      "incompleta":        "boolean (true si campos_pendientes.length > 0)",
      "campos_pendientes": "array — campos de {nombre, ingredientes, instrucciones, porciones} que esten vacios",
      "created_at":        "ISO 8601",
      "updated_at":        "ISO 8601"
    },
    "shape_ingrediente_catalogado": {
      "nombre":         "string trim — clave logica (lookup case-insensitive)",
      "categoria":      "string | null",
      "unidad":         "string | null",
      "precio_mercado": "number >= 0 (eur por unidad)",
      "fuente":         "string in {manual, scrap, api, agente} — default 'manual'",
      "created_at":     "ISO 8601",
      "updated_at":     "ISO 8601"
    }
  },

  "fuentes_aceptadas": {
    "receta":      ["manual", "importada", "sugerida", "agente"],
    "precio":      ["manual", "scrap", "api", "agente"]
  },

  "normalizadores_que_aplica_el_llm": {
    "_descripcion": "El LLM razona estos normalizadores mentalmente — son lenguaje natural + reglas. No son tools.",
    "nombre": "trim. Comparacion para duplicados: toLowerCase + trim.",
    "ingredientes": [
      "Si input es array de strings → cada string es { nombre, cantidad: null, unidad: null, notas: '' }.",
      "Si input es array de objetos → mapear sinonimos (name→nombre, quantity→cantidad, unit→unidad, notes→notas). Default notas: ''.",
      "Si input es string libre con saltos de linea, ; o , → split + cada item como { nombre, cantidad: null, unidad: null, notas: '' }.",
      "Filtrar al final items con nombre vacio."
    ],
    "instrucciones": [
      "Si input es array → trim cada string, filtrar vacios.",
      "Si input es string → split por '\\n' o '. ' (punto+espacio), trim, filtrar vacios."
    ],
    "porciones":  "number > 0 → preservar. Otro → null.",
    "tiempo_min": "number >= 0 → preservar. Otro → null.",
    "dificultad": "number → preservar tal cual. Otro → null.",
    "fuente_receta": "Si input en {manual,importada,sugerida,agente} → preservar. Otro → 'manual'.",
    "fuente_precio": "Si input en {manual,scrap,api,agente} → preservar. Otro → 'manual'."
  },

  "campos_que_marcan_completa": ["nombre", "ingredientes", "instrucciones", "porciones"],

  "operaciones": {

    "crear": {
      "input": "{ project_id (uuid, req), user_id?, nombre (req, string trim), ingredientes?, instrucciones?, porciones?, tiempo_min?, dificultad?, categorias?, etiquetas?, fuente?, descripcion?, notas?, correlation_id? }",
      "pseudocodigo": [
        "async crear(input):",
        "  // 1. Validacion minima",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.nombre || input.nombre.trim() === '') return INVALID_INPUT { field: 'nombre' }",
        "",
        "  // 2. Leer el store (path RELATIVO — filesystem resuelve via project_id).",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  store = raw.status === 404",
        "    ? { _version: '1.0', _updated_at: nowISO(), recetas: [], ingredientes_catalogo: [] }",
        "    : JSON.parse(raw.content)",
        "",
        "  // 3. Regla: no duplicado activo (nombre normalizado igual en_servicio).",
        "  duplicado = store.recetas.find(r =>",
        "    r.nombre.toLowerCase().trim() === input.nombre.toLowerCase().trim()",
        "    && r.estado_operativo === 'en_servicio')",
        "  if (duplicado) return ALREADY_EXISTS { entity_type: 'recipe', existing_id: duplicado.id }",
        "",
        "  // 4. Normalizar (TU lo haces — lenguaje natural).",
        "  ingredientes_norm  = <normalizar input.ingredientes>",
        "  instrucciones_norm = <normalizar input.instrucciones>",
        "",
        "  // 5. Construir receta canonica.",
        "  receta = {",
        "    id: uuid(),  // TU generas. CAVEAT: el LLM aluciña uuids — sugerencia futura: primitiva bus.newId.",
        "    nombre: input.nombre.trim(),",
        "    descripcion: input.descripcion || '',",
        "    ingredientes: ingredientes_norm,",
        "    instrucciones: instrucciones_norm,",
        "    porciones: typeof input.porciones === 'number' && input.porciones > 0 ? input.porciones : null,",
        "    tiempo_min: typeof input.tiempo_min === 'number' && input.tiempo_min >= 0 ? input.tiempo_min : null,",
        "    dificultad: typeof input.dificultad === 'number' ? input.dificultad : null,",
        "    categorias: Array.isArray(input.categorias) ? input.categorias.map(String) : [],",
        "    etiquetas: Array.isArray(input.etiquetas) ? input.etiquetas.map(String) : [],",
        "    fuente: ['manual','importada','sugerida','agente'].includes(input.fuente) ? input.fuente : 'manual',",
        "    notas: input.notas || '',",
        "    estado_operativo: 'en_servicio',  // recalculado paso 6",
        "    version: 1,",
        "    history: [],",
        "    incompleta: false,",
        "    campos_pendientes: [],",
        "    created_at: nowISO(),",
        "    updated_at: nowISO()",
        "  }",
        "",
        "  // 6. Completitud + estado inicial.",
        "  receta.campos_pendientes = [ campos en {nombre, ingredientes, instrucciones, porciones} vacios en receta ]",
        "  receta.incompleta = receta.campos_pendientes.length > 0",
        "  if (receta.incompleta) receta.estado_operativo = 'borrador'",
        "",
        "  // 7. Append + persistir atomicamente.",
        "  store.recetas.push(receta)",
        "  store._updated_at = nowISO()",
        "  await publishAndWait('fs.write.request', {",
        "    project_id: input.project_id,",
        "    path: '/recetas.json',",
        "    content: JSON.stringify(store, null, 2),",
        "    encoding: 'utf-8',",
        "    atomic: true",
        "  })",
        "",
        "  // 8. Evento de dominio.",
        "  publish('receta.creada', {",
        "    receta_id: receta.id, nombre: receta.nombre, version: 1,",
        "    estado_operativo: receta.estado_operativo,",
        "    ...(receta.incompleta ? { incompleta: true, campos_pendientes: receta.campos_pendientes } : {})",
        "  })",
        "",
        "  // 9. Response canonica al caller.",
        "  return { status: 201, data: {",
        "    receta_id: receta.id, nombre: receta.nombre, version: 1,",
        "    estado_operativo: receta.estado_operativo,",
        "    incompleta: receta.incompleta, campos_pendientes: receta.campos_pendientes",
        "  } }"
      ],
      "reglas_clave": [
        "No duplicado activo (nombre normalizado + en_servicio).",
        "Estado inicial: 'borrador' si campos_pendientes.length>0, 'en_servicio' si no.",
        "publish receta.creada SOLO tras fs.write OK. Si write falla, return UPSTREAM_TIMEOUT/UNREACHABLE sin publicar."
      ],
      "errores_posibles": ["INVALID_INPUT", "ALREADY_EXISTS", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "listar": {
      "input": "{ project_id (req), estado?='en_servicio', solo_incompletas?=false, limit?=50 }",
      "pseudocodigo": [
        "async listar(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  estado = input.estado || 'en_servicio'",
        "  if (!['borrador','en_servicio','archivada'].includes(estado)) return INVALID_INPUT { field: 'estado' }",
        "  limit = (typeof input.limit === 'number' && input.limit > 0) ? input.limit : 50",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return { status: 200, data: { total: 0, recetas: [] } }",
        "  store = JSON.parse(raw.content)",
        "",
        "  items = store.recetas.filter(r => r.estado_operativo === estado)",
        "  if (input.solo_incompletas === true) items = items.filter(r => r.incompleta === true)",
        "",
        "  return { status: 200, data: {",
        "    total: items.length,",
        "    recetas: items.slice(0, limit).map(r => ({",
        "      receta_id: r.id, nombre: r.nombre, porciones: r.porciones, dificultad: r.dificultad,",
        "      incompleta: r.incompleta, campos_pendientes: r.campos_pendientes,",
        "      estado_operativo: r.estado_operativo, version: r.version, updated_at: r.updated_at",
        "    }))",
        "  } }"
      ],
      "reglas_clave": ["Solo lee, no muta. No publica eventos."],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "obtener": {
      "input": "{ project_id (req), receta_id? | nombre? (al menos uno req) }",
      "pseudocodigo": [
        "async obtener(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.receta_id && !input.nombre) return INVALID_INPUT { field: 'receta_id|nombre' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "  store = JSON.parse(raw.content)",
        "",
        "  // Lookup por id (UUID) → comparar id. Sino por nombre normalizado.",
        "  ref = input.receta_id || input.nombre",
        "  esUuid = <ref matches UUIDv4 regex>",
        "  r = esUuid",
        "    ? store.recetas.find(x => x.id === ref)",
        "    : store.recetas.find(x => x.nombre.toLowerCase().trim() === ref.toLowerCase().trim())",
        "  if (!r) return RESOURCE_NOT_FOUND { entity_type: 'recipe', entity_ref: ref }",
        "",
        "  // Devolver receta sin history pero anyadiendo versiones_anteriores (count).",
        "  { history, ...rest } = r",
        "  return { status: 200, data: { ...rest, versiones_anteriores: history.length } }"
      ],
      "reglas_clave": ["Solo lee. Recetas archivadas TAMBIEN se devuelven (filtrar es responsabilidad del caller)."],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "buscar": {
      "input": "{ project_id (req), texto?, ingrediente?, categoria?, etiqueta?, dificultad_min?, dificultad_max?, tiempo_max?, porciones?, limit?=20 }",
      "pseudocodigo": [
        "async buscar(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  limit = (typeof input.limit === 'number' && input.limit > 0) ? input.limit : 20",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return { status: 200, data: { total: 0, resultados: [] } }",
        "  store = JSON.parse(raw.content)",
        "",
        "  // Solo busca entre en_servicio.",
        "  items = store.recetas.filter(r => r.estado_operativo === 'en_servicio')",
        "  if (input.texto) {",
        "    t = input.texto.toLowerCase()",
        "    items = items.filter(r => r.nombre.toLowerCase().includes(t) || r.descripcion.toLowerCase().includes(t) || r.ingredientes.some(ing => ing.nombre.toLowerCase().includes(t)))",
        "  }",
        "  if (input.ingrediente) {",
        "    ing = input.ingrediente.toLowerCase()",
        "    items = items.filter(r => r.ingredientes.some(i => i.nombre.toLowerCase().includes(ing)))",
        "  }",
        "  if (input.categoria)        items = items.filter(r => r.categorias.includes(input.categoria))",
        "  if (input.etiqueta)         items = items.filter(r => r.etiquetas.includes(input.etiqueta))",
        "  if (typeof input.dificultad_min === 'number') items = items.filter(r => typeof r.dificultad === 'number' && r.dificultad >= input.dificultad_min)",
        "  if (typeof input.dificultad_max === 'number') items = items.filter(r => typeof r.dificultad === 'number' && r.dificultad <= input.dificultad_max)",
        "  if (typeof input.tiempo_max === 'number')     items = items.filter(r => typeof r.tiempo_min === 'number' && r.tiempo_min <= input.tiempo_max)",
        "  if (typeof input.porciones === 'number')      items = items.filter(r => r.porciones === input.porciones)",
        "",
        "  return { status: 200, data: {",
        "    total: items.length,",
        "    resultados: items.slice(0, limit).map(r => ({",
        "      receta_id: r.id, nombre: r.nombre, porciones: r.porciones, dificultad: r.dificultad,",
        "      tiempo_min: r.tiempo_min, categorias: r.categorias, etiquetas: r.etiquetas",
        "    }))",
        "  } }"
      ],
      "reglas_clave": ["Filtros AND. Solo busca entre en_servicio. Devuelve ids+resumen, no recetas completas."],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "actualizar": {
      "input": "{ project_id (req), receta_id (req), cambios (req, objeto con campos a modificar), correlation_id? }",
      "pseudocodigo": [
        "async actualizar(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.receta_id)  return INVALID_INPUT { field: 'receta_id' }",
        "  if (!input.cambios || typeof input.cambios !== 'object' || Array.isArray(input.cambios)) return INVALID_INPUT { field: 'cambios' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "  store = JSON.parse(raw.content)",
        "  r = store.recetas.find(x => x.id === input.receta_id)",
        "  if (!r) return RESOURCE_NOT_FOUND { entity_type: 'recipe', entity_ref: input.receta_id }",
        "",
        "  // Snapshot a history ANTES de aplicar cambios.",
        "  { history: _h, ...snapshot } = r",
        "  r.history.push({ ...snapshot, _archived_at: nowISO() })",
        "",
        "  aplicados = []",
        "  // Whitelist de campos modificables + sus normalizadores.",
        "  for (campo in input.cambios) {",
        "    switch (campo) {",
        "      case 'nombre':        r.nombre = String(input.cambios.nombre).trim(); aplicados.push('nombre')",
        "      case 'descripcion':   r.descripcion = typeof input.cambios.descripcion === 'string' ? input.cambios.descripcion : ''",
        "      case 'porciones':     r.porciones = typeof v === 'number' && v > 0 ? v : null",
        "      case 'tiempo_min':    r.tiempo_min = typeof v === 'number' && v >= 0 ? v : null",
        "      case 'dificultad':    r.dificultad = typeof v === 'number' ? v : null",
        "      case 'notas':         r.notas = typeof v === 'string' ? v : ''",
        "      case 'fuente':        if (['manual','importada','sugerida','agente'].includes(v)) r.fuente = v",
        "      case 'ingredientes':  r.ingredientes = <normalizar input.cambios.ingredientes>",
        "      case 'instrucciones': r.instrucciones = <normalizar input.cambios.instrucciones>",
        "      case 'categorias':    r.categorias = Array.isArray(v) ? v.map(String) : []",
        "      case 'etiquetas':     r.etiquetas  = Array.isArray(v) ? v.map(String) : []",
        "      default: <ignorar>",
        "    }",
        "  }",
        "",
        "  if (aplicados.length === 0) {",
        "    r.history.pop()  // revertir snapshot inutil",
        "    return INVALID_INPUT { field: 'cambios', message: 'ningun campo conocido' }",
        "  }",
        "",
        "  r.version += 1",
        "  r.updated_at = nowISO()",
        "  // Recalcular completitud.",
        "  r.campos_pendientes = [ campos en {nombre,ingredientes,instrucciones,porciones} vacios ]",
        "  r.incompleta = r.campos_pendientes.length > 0",
        "",
        "  store._updated_at = nowISO()",
        "  await publishAndWait('fs.write.request', { project_id, path: '/recetas.json', content: JSON.stringify(store, null, 2), encoding: 'utf-8', atomic: true })",
        "",
        "  publish('receta.actualizada', {",
        "    receta_id: r.id, nombre: r.nombre, version: r.version,",
        "    campos_actualizados: aplicados,",
        "    ...(r.incompleta ? { incompleta: true, campos_pendientes: r.campos_pendientes } : {})",
        "  })",
        "",
        "  return { status: 200, data: {",
        "    receta_id: r.id, nombre: r.nombre, version: r.version,",
        "    campos_actualizados: aplicados,",
        "    incompleta: r.incompleta, campos_pendientes: r.campos_pendientes",
        "  } }"
      ],
      "reglas_clave": [
        "Solo campos whitelist se aplican (los listados en el switch). Otros se ignoran silenciosamente.",
        "Si ningun campo conocido en cambios → INVALID_INPUT (no persistir, no bumpear version).",
        "Snapshot a history ANTES de aplicar cambios. Si no hay cambios validos, revertir el snapshot (pop).",
        "version += 1. updated_at refrescado. Recalcular incompleta + campos_pendientes."
      ],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "historial": {
      "input": "{ project_id (req), receta_id (req) }",
      "pseudocodigo": [
        "async historial(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.receta_id)  return INVALID_INPUT { field: 'receta_id' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "  store = JSON.parse(raw.content)",
        "  r = store.recetas.find(x => x.id === input.receta_id)",
        "  if (!r) return RESOURCE_NOT_FOUND { entity_type: 'recipe', entity_ref: input.receta_id }",
        "",
        "  versiones = (r.history || []).map(h => ({",
        "    version: h.version,",
        "    archived_at: h._archived_at,",
        "    nombre: h.nombre, porciones: h.porciones, dificultad: h.dificultad,",
        "    ingredientes_count: Array.isArray(h.ingredientes) ? h.ingredientes.length : 0",
        "  }))",
        "",
        "  return { status: 200, data: {",
        "    receta_id: r.id, nombre: r.nombre,",
        "    version_actual: r.version,",
        "    versiones_anteriores: versiones.length,",
        "    historial: versiones",
        "  } }"
      ],
      "reglas_clave": ["Solo lee. Devuelve resumen de cada snapshot del history."],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "revertir": {
      "input": "{ project_id (req), receta_id (req), target_version (req, entero >=1) }",
      "pseudocodigo": [
        "async revertir(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.receta_id)  return INVALID_INPUT { field: 'receta_id' }",
        "  if (!Number.isInteger(input.target_version) || input.target_version < 1) return INVALID_INPUT { field: 'target_version' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "  store = JSON.parse(raw.content)",
        "  r = store.recetas.find(x => x.id === input.receta_id)",
        "  if (!r) return RESOURCE_NOT_FOUND { entity_type: 'recipe', entity_ref: input.receta_id }",
        "",
        "  target = (r.history || []).find(h => h.version === input.target_version)",
        "  if (!target) return RESOURCE_NOT_FOUND { entity_type: 'recipe-version', entity_ref: input.target_version, versiones_disponibles: r.history.map(h => h.version) }",
        "",
        "  // Snapshot del estado actual ANTES de revertir.",
        "  { history: _h, ...currentSnap } = r",
        "  r.history.push({ ...currentSnap, _archived_at: nowISO() })",
        "",
        "  // Aplicar target (sin sobreescribir version + history).",
        "  { _archived_at, version: _v, ...restore } = target",
        "  Object.assign(r, restore)",
        "  r.version += 1  // bumps respecto al actual, no al target",
        "  r.updated_at = nowISO()",
        "  // Recalcular completitud (puede haber cambiado).",
        "  r.campos_pendientes = [...]",
        "  r.incompleta = r.campos_pendientes.length > 0",
        "",
        "  store._updated_at = nowISO()",
        "  await publishAndWait('fs.write.request', { project_id, path: '/recetas.json', content: JSON.stringify(store, null, 2), encoding: 'utf-8', atomic: true })",
        "",
        "  publish('receta.actualizada', {",
        "    receta_id: r.id, nombre: r.nombre, version: r.version, motivo: 'revertir',",
        "    ...(r.incompleta ? { incompleta: true, campos_pendientes: r.campos_pendientes } : {})",
        "  })",
        "",
        "  return { status: 200, data: {",
        "    receta_id: r.id, nombre: r.nombre,",
        "    revertida_a_version: input.target_version,",
        "    version_actual: r.version",
        "  } }"
      ],
      "reglas_clave": [
        "El revertido crea una NUEVA version (no decrementa). El estado anterior se guarda en history primero.",
        "Si target_version no existe → RESOURCE_NOT_FOUND con lista de versiones disponibles."
      ],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "eliminar": {
      "input": "{ project_id (req), receta_id (req), motivo?: string }",
      "_descripcion_semantica": "Eliminacion LOGICA (estado → archivada). No borra del store. Equivalente a cambiar_estado(target='archivada') pero con evento extra receta.eliminada.",
      "pseudocodigo": [
        "async eliminar(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.receta_id)  return INVALID_INPUT { field: 'receta_id' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "  store = JSON.parse(raw.content)",
        "  r = store.recetas.find(x => x.id === input.receta_id)",
        "  if (!r) return RESOURCE_NOT_FOUND { entity_type: 'recipe', entity_ref: input.receta_id }",
        "",
        "  if (r.estado_operativo === 'archivada') {",
        "    return { status: 200, data: { receta_id: r.id, nombre: r.nombre, estado_operativo: 'archivada', status: 'ya_estaba_archivada' } }",
        "  }",
        "",
        "  estado_anterior = r.estado_operativo",
        "  r.estado_operativo = 'archivada'",
        "  r.updated_at = nowISO()",
        "  store._updated_at = nowISO()",
        "  await publishAndWait('fs.write.request', { project_id, path: '/recetas.json', content: JSON.stringify(store, null, 2), encoding: 'utf-8', atomic: true })",
        "",
        "  publish('receta.estado.actualizada', {",
        "    receta_id: r.id, nombre: r.nombre,",
        "    estado_anterior, estado_nuevo: 'archivada', version: r.version,",
        "    ...(input.motivo ? { motivo: input.motivo } : {})",
        "  })",
        "  publish('receta.eliminada', {",
        "    receta_id: r.id, nombre: r.nombre,",
        "    ...(input.motivo ? { motivo: input.motivo } : {})",
        "  })",
        "",
        "  return { status: 200, data: { receta_id: r.id, nombre: r.nombre, estado_operativo: 'archivada', status: 'archivada' } }"
      ],
      "reglas_clave": [
        "Si ya esta archivada → idempotente (return 'ya_estaba_archivada', sin publicar eventos).",
        "Publica DOS eventos: receta.estado.actualizada (cambio de estado) + receta.eliminada (eliminacion logica)."
      ],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "cambiar_estado": {
      "input": "{ project_id (req), receta_id (req), target_estado (req in {borrador|en_servicio|archivada}), motivo?: string }",
      "pseudocodigo": [
        "async cambiar_estado(input):",
        "  if (!input.project_id || !input.receta_id || !input.target_estado) return INVALID_INPUT",
        "  if (!['borrador','en_servicio','archivada'].includes(input.target_estado)) return INVALID_INPUT { field: 'target_estado' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "  store = JSON.parse(raw.content)",
        "  r = store.recetas.find(x => x.id === input.receta_id)",
        "  if (!r) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "",
        "  estado_anterior = r.estado_operativo",
        "  if (estado_anterior === input.target_estado) {",
        "    return { status: 200, data: { receta_id: r.id, estado_operativo: estado_anterior, status: 'sin_cambios' } }",
        "  }",
        "",
        "  // Transiciones validas (declaradas en el blueprint padre estado_operativo.transiciones_validas).",
        "  permitidas = {",
        "    'borrador':    ['en_servicio'],",
        "    'en_servicio': ['borrador', 'archivada'],",
        "    'archivada':   ['en_servicio']",
        "  }[estado_anterior]",
        "  if (!permitidas.includes(input.target_estado)) {",
        "    return PRECONDITION_FAILED {",
        "      kind: 'invalid_state_transition',",
        "      estado_anterior, target_estado: input.target_estado,",
        "      transiciones_permitidas: permitidas",
        "    }",
        "  }",
        "",
        "  r.estado_operativo = input.target_estado",
        "  r.updated_at = nowISO()",
        "  store._updated_at = nowISO()",
        "  await publishAndWait('fs.write.request', { project_id, path: '/recetas.json', content: JSON.stringify(store, null, 2), encoding: 'utf-8', atomic: true })",
        "",
        "  publish('receta.estado.actualizada', {",
        "    receta_id: r.id, nombre: r.nombre,",
        "    estado_anterior, estado_nuevo: input.target_estado, version: r.version,",
        "    ...(input.motivo ? { motivo: input.motivo } : {})",
        "  })",
        "  if (input.target_estado === 'archivada') {",
        "    publish('receta.eliminada', { receta_id: r.id, nombre: r.nombre, ...(input.motivo ? { motivo: input.motivo } : {}) })",
        "  }",
        "",
        "  return { status: 200, data: { receta_id: r.id, estado_anterior, estado_operativo: r.estado_operativo } }"
      ],
      "reglas_clave": [
        "Transiciones validas declaradas en blueprint padre. PRECONDITION_FAILED si no permitida.",
        "Si target_estado === estado_actual → idempotente (status: 'sin_cambios', sin eventos).",
        "Si target_estado === 'archivada' → publish ADICIONAL receta.eliminada (compat semantica con eliminar)."
      ],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "PRECONDITION_FAILED", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "estadisticas": {
      "input": "{ project_id (req) }",
      "pseudocodigo": [
        "async estadisticas(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return { status: 200, data: { total_recetas: 0, por_estado: {borrador:0, en_servicio:0, archivada:0}, incompletas: 0, ingredientes_catalogo: 0, ingredientes_con_precio: 0, ingredientes_usados_unicos: 0 } }",
        "  store = JSON.parse(raw.content)",
        "",
        "  por_estado = { borrador: 0, en_servicio: 0, archivada: 0 }",
        "  incompletas = 0",
        "  ingredientes_usados = new Set()",
        "  for (r in store.recetas) {",
        "    por_estado[r.estado_operativo]++",
        "    if (r.incompleta) incompletas++",
        "    for (ing in r.ingredientes) ingredientes_usados.add(ing.nombre.toLowerCase())",
        "  }",
        "  con_precio = store.ingredientes_catalogo.filter(i => typeof i.precio_mercado === 'number' && i.precio_mercado > 0).length",
        "",
        "  return { status: 200, data: {",
        "    total_recetas: store.recetas.length,",
        "    por_estado, incompletas,",
        "    ingredientes_catalogo: store.ingredientes_catalogo.length,",
        "    ingredientes_con_precio: con_precio,",
        "    ingredientes_usados_unicos: ingredientes_usados.size",
        "  } }"
      ],
      "reglas_clave": ["Solo lee. No publica."],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "ingredientes": {
      "input": "{ project_id (req), categoria? }",
      "pseudocodigo": [
        "async ingredientes(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return { status: 200, data: { total: 0, ingredientes: [] } }",
        "  store = JSON.parse(raw.content)",
        "",
        "  arr = store.ingredientes_catalogo",
        "  if (input.categoria) arr = arr.filter(i => i.categoria === input.categoria)",
        "  return { status: 200, data: { total: arr.length, ingredientes: arr } }"
      ],
      "reglas_clave": ["Solo lee el catalogo. Filtro opcional por categoria."],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "actualizar_precio": {
      "input": "{ project_id (req), nombre (req), precio_mercado (req, number >= 0), unidad?, categoria?, fuente? in {manual,scrap,api,agente} }",
      "_descripcion_semantica": "Upsert sobre ingredientes_catalogo por nombre normalizado. Si no existe el ingrediente, lo crea. Si existe, actualiza precio + campos opcionales preservando los no enviados.",
      "pseudocodigo": [
        "async actualizar_precio(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.nombre || input.nombre.trim() === '') return INVALID_INPUT { field: 'nombre' }",
        "  if (typeof input.precio_mercado !== 'number' || input.precio_mercado < 0 || isNaN(input.precio_mercado)) return INVALID_INPUT { field: 'precio_mercado' }",
        "  if (input.fuente !== undefined && !['manual','scrap','api','agente'].includes(input.fuente)) return INVALID_INPUT { field: 'fuente' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  store = raw.status === 404",
        "    ? { _version: '1.0', _updated_at: nowISO(), recetas: [], ingredientes_catalogo: [] }",
        "    : JSON.parse(raw.content)",
        "",
        "  nombreTrim = input.nombre.trim()",
        "  lower = nombreTrim.toLowerCase()",
        "  item = store.ingredientes_catalogo.find(i => i.nombre.toLowerCase() === lower)",
        "  now = nowISO()",
        "",
        "  if (!item) {",
        "    item = {",
        "      nombre: nombreTrim,",
        "      categoria: typeof input.categoria === 'string' ? input.categoria : null,",
        "      unidad:    typeof input.unidad    === 'string' ? input.unidad    : null,",
        "      precio_mercado: input.precio_mercado,",
        "      fuente: ['manual','scrap','api','agente'].includes(input.fuente) ? input.fuente : 'manual',",
        "      created_at: now, updated_at: now",
        "    }",
        "    store.ingredientes_catalogo.push(item)",
        "  } else {",
        "    if (typeof input.categoria === 'string') item.categoria = input.categoria",
        "    if (typeof input.unidad === 'string')    item.unidad    = input.unidad",
        "    if (['manual','scrap','api','agente'].includes(input.fuente)) item.fuente = input.fuente",
        "    item.precio_mercado = input.precio_mercado",
        "    item.updated_at = now",
        "  }",
        "",
        "  store._updated_at = nowISO()",
        "  await publishAndWait('fs.write.request', { project_id, path: '/recetas.json', content: JSON.stringify(store, null, 2), encoding: 'utf-8', atomic: true })",
        "",
        "  publish('ingrediente.precio.actualizado', {",
        "    nombre: item.nombre, precio_mercado: item.precio_mercado,",
        "    ...(item.unidad ? { unidad: item.unidad } : {}),",
        "    ...(item.categoria ? { categoria: item.categoria } : {}),",
        "    ...(item.fuente ? { fuente: item.fuente } : {})",
        "  })",
        "",
        "  return { status: 200, data: { nombre: item.nombre, precio_mercado: item.precio_mercado, unidad: item.unidad, status: 'actualizado' } }"
      ],
      "reglas_clave": [
        "Upsert: crear si no existe, actualizar si existe.",
        "Lookup case-insensitive por nombre normalizado (toLowerCase).",
        "Campos opcionales no enviados PRESERVAN el valor anterior (no se sobreescriben con null)."
      ],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "analizar": {
      "input": "{ project_id (req), receta_id (req) }",
      "_descripcion_semantica": "Calcula el coste de una receta cruzando sus ingredientes con el catalogo de precios. NO modifica nada. Si la receta esta incompleta o si faltan precios/unidades en el catalogo, lo refleja en la response (coste_es_real=false).",
      "pseudocodigo": [
        "async analizar(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.receta_id)  return INVALID_INPUT { field: 'receta_id' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'recipe' }",
        "  store = JSON.parse(raw.content)",
        "  r = store.recetas.find(x => x.id === input.receta_id)",
        "  if (!r) return RESOURCE_NOT_FOUND { entity_type: 'recipe', entity_ref: input.receta_id }",
        "  if (r.incompleta) return PRECONDITION_FAILED { kind: 'incomplete_recipe', campos_pendientes: r.campos_pendientes }",
        "",
        "  coste_total = 0",
        "  coste_es_real = true",
        "  desglose = []",
        "  for (ing in r.ingredientes) {",
        "    cat = store.ingredientes_catalogo.find(i => i.nombre.toLowerCase() === ing.nombre.toLowerCase())",
        "    coste = null",
        "    if (cat && typeof cat.precio_mercado === 'number' && cat.unidad === ing.unidad && typeof ing.cantidad === 'number') {",
        "      coste = ing.cantidad * cat.precio_mercado",
        "      coste_total += coste",
        "    } else {",
        "      coste_es_real = false",
        "    }",
        "    desglose.push({ nombre: ing.nombre, cantidad: ing.cantidad, unidad: ing.unidad, precio_mercado: cat ? cat.precio_mercado : null, coste, en_catalogo: !!cat })",
        "  }",
        "  porciones = r.porciones || 1",
        "  cpp = coste_es_real ? (coste_total / porciones) : null",
        "",
        "  return { status: 200, data: {",
        "    receta_id: r.id, nombre: r.nombre, porciones: r.porciones, tiempo_min: r.tiempo_min, dificultad: r.dificultad,",
        "    ingredientes: desglose,",
        "    coste_total: coste_es_real ? coste_total : null,",
        "    coste_por_porcion: cpp,",
        "    coste_es_real,",
        "    nota: coste_es_real ? null : 'Faltan precios o unidades incompatibles en el catalogo para algunos ingredientes.'",
        "  } }"
      ],
      "reglas_clave": [
        "Solo lee, no muta. No publica eventos.",
        "Si receta.incompleta → PRECONDITION_FAILED (rechaza analizar incompletas).",
        "coste_es_real=true SOLO si todos los ingredientes tienen precio + unidad compatible. Sino, coste_total=null y nota explicativa."
      ],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "PRECONDITION_FAILED", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    },

    "investigar_receta": {
      "input": "{ project_id (req), nombre_receta (req) }",
      "_descripcion_semantica": "Comprueba si una receta existe por nombre. Si existe la devuelve. Si no, devuelve una instruccion al LLM caller para que proponga + confirme con el usuario antes de crear. NO crea automaticamente.",
      "pseudocodigo": [
        "async investigar_receta(input):",
        "  if (!input.project_id)    return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.nombre_receta) return INVALID_INPUT { field: 'nombre_receta' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/recetas.json' })",
        "  if (raw.status === 404) return { status: 200, data: { existe: false, instruccion: 'Store vacio. Propon una receta y guardala con crear tras confirmacion del usuario.' } }",
        "  store = JSON.parse(raw.content)",
        "",
        "  lower = input.nombre_receta.toLowerCase().trim()",
        "  r = store.recetas.find(x => x.nombre.toLowerCase().trim() === lower)",
        "  if (r) {",
        "    { history, ...rest } = r",
        "    return { status: 200, data: { existe: true, receta: rest } }",
        "  }",
        "  return { status: 200, data: { existe: false, instruccion: 'No existe en el proyecto. Propon una receta y guardala con crear tras confirmacion del usuario.' } }"
      ],
      "reglas_clave": [
        "Solo lee. No publica. No crea recetas automaticamente.",
        "Si no existe → devuelve instruccion textual para que el LLM caller la siga (proponer + confirmar antes de crear)."
      ],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE", "UNKNOWN_ERROR"]
    }

  },

  "eventos_publicados": [
    "receta.creada",
    "receta.actualizada",
    "receta.estado.actualizada",
    "receta.eliminada",
    "ingrediente.precio.actualizado"
  ],

  "eventos_que_escucho": [],

  "trabajo_pendiente": [
    "Primitiva universal del bus 'bus.newId' o 'uuid.generate.request/response' que devuelva UUIDv4 real generado por crypto.randomUUID() — para reemplazar el helper LLM-side uuid() que aluciña ids con shape regular.",
    "Schemas JSON Schema 2020-12 estrictos para los 5 eventos publicados, integrados en subsistema-recetario.contract.json + validator.",
    "Validacion estricta de catalogo en ai-gateway._executeToolCall — actualmente el LLM puede emitir cualquier nombre de tool (recetas.crear, etc.) y se publica al bus sin validar. Permite saltarse el blueprint cuando el modulo legacy sigue cargado."
  ]
}
