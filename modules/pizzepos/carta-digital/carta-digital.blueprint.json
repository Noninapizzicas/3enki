{
  "_doc": "Blueprint del modulo carta-digital. Hijo de subsistema-recetario.modulo-base. Backoffice de la carta publica: branding del proyecto + carta compuesta (datos de carta-manager + ofertas + marketing). 4 operaciones (get_config, update_config, get_carta_publica, set_carta_compuesta). Composicion real (PWA build, ensamblaje de ofertas con copy) la disparan agentes via agent.execute.request — el blueprint solo gestiona la config y el estado compuesto persistido.",

  "id": "carta-digital",
  "extends_blueprint_abstract": "subsistema-recetario.modulo-base",
  "version": "blueprint-1.2.0",
  "_v1_2_changelog": "v1.2.0 (2026-06-01): aplica contrato subsistema-carta v1.0.1. Path canonico /storage/pizzepos/carta-digital/config.json en TODAS las operaciones (get_config, update_config, get_carta_publica, set_carta_compuesta) reemplazando /carta-digital.json pelado. correlation_id obligatorio en payloads de cartadigital.config.actualizada (update_config) y cartadigital.carta_publica.actualizada (set_carta_compuesta) --garantizado con uuid() si el input no lo trae. eventos_que_escucho:[] preservado --decision A1 del horizonte, el blueprint conserva su modelo no-reactivo cocinado en v1.1.0.",
  "language": "es",

  "rol": "Backoffice de la carta publica del cliente. Custodio del archivo de config (branding, dominio publico, opciones) + la carta compuesta final (snapshot listo para servir). NO compone — eso lo hacen los agentes cartadigital-composer / ofertas / reviewer cuando el LLM les delega via agent.execute.request.",

  "garantiza": [
    "Config de carta digital persistida en /storage/pizzepos/carta-digital/config.json del proyecto.",
    "get_config / update_config son CRUD sobre ese archivo.",
    "set_carta_compuesta guarda el snapshot final (compuesto por agentes) listo para servir.",
    "get_carta_publica devuelve el snapshot compuesto tal cual."
  ],

  "no_garantiza": [
    "Composicion creativa — eso lo hace el agente cartadigital-composer.",
    "Generacion PWA build — eso lo hace cartadigital-pwa-builder (agente).",
    "Generacion de ofertas — agente cartadigital-ofertas.",
    "Reaccion automatica a carta.actualizada / tarifas.config.actualizada — el blueprint no es reactivo en background. El LLM en page=carta-digital decide cuando recomponer."
  ],

  "estado_persistente": {
    "paths_relativos": {
      "config": "/storage/pizzepos/carta-digital/config.json"
    },
    "shape_config": "{ _version, _updated_at, branding: {nombre, lema, colores, logo_url, fuente, ...}, dominio_publico, contacto: {telefono, email, web, redes}, opciones_visualizacion, carta_compuesta: {meta, categorias, productos, ofertas, generado_at, generado_por} }"
  },

  "eventos_publicados": [
    "cartadigital.get_config.response", "cartadigital.get_config.failed",
    "cartadigital.update_config.response", "cartadigital.update_config.failed",
    "cartadigital.get_carta_publica.response", "cartadigital.get_carta_publica.failed",
    "cartadigital.set_carta_compuesta.response", "cartadigital.set_carta_compuesta.failed",
    "cartadigital.config.actualizada", "cartadigital.carta_publica.actualizada"
  ],

  "eventos_que_escucho": [],

  "operaciones": {
    "get_config": {
      "input": "{ project_id (req), request_id (req), correlation_id? }",
      "pseudocodigo": [
        "async get_config(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json' })",
        "  if (raw.status === 404) return { status: 200, data: { _version: '1.0', _updated_at: nowISO(), branding: {}, opciones_visualizacion: {}, carta_compuesta: null } }",
        "  if (raw.status >= 400) return raw",
        "  return { status: 200, data: JSON.parse(raw.content) }"
      ],
      "reglas_clave": ["Sin config → devuelve config-vacia default (no error)."],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE"]
    },

    "update_config": {
      "input": "{ project_id (req), request_id (req), correlation_id?, user_id?, campos (req — object con branding/dominio/contacto/opciones a modificar) }",
      "pseudocodigo": [
        "async update_config(input):",
        "  if (!input.project_id || !input.campos || Object.keys(input.campos).length === 0) return INVALID_INPUT",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json' })",
        "  if (raw.status >= 400 && raw.status !== 404) return raw",
        "  now = nowISO()",
        "",
        "  //    Rama A — archivo NO existe: fs.write atomico del store inicial con los campos recibidos.",
        "  //    Rama B — archivo existe: fs.edit declarativo (RFC 6902). Patron UPDATE compuesto:",
        "  //      Para cada campo en input.campos aplica merge y construye patch op:replace /<campo>.",
        "  //      Anyade op:replace /_updated_at al final. No compone el archivo entero.",
        "  if (raw.status === 404) {",
        "    config = { _version: '1.0', _updated_at: now, branding: {}, opciones_visualizacion: {}, carta_compuesta: null }",
        "    // Merge superficial top-level / profundo en sub-objetos.",
        "    for (k in input.campos) {",
        "      if (typeof input.campos[k] === 'object' && input.campos[k] !== null && !Array.isArray(input.campos[k]) && typeof config[k] === 'object') {",
        "        merged = {}",
        "        for (subk in (config[k] || {})) { merged[subk] = config[k][subk] }",
        "        for (subk in input.campos[k]) { merged[subk] = input.campos[k][subk] }",
        "        config[k] = merged",
        "      } else {",
        "        config[k] = input.campos[k]",
        "      }",
        "    }",
        "    config._updated_at = now",
        "    write_resp = await publishAndWait('fs.write.request', {",
        "      project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json',",
        "      content: JSON.stringify(config, null, 2), encoding: 'utf-8', atomic: true",
        "    })",
        "    if (write_resp.status >= 400) return write_resp",
        "  } else {",
        "    config = JSON.parse(raw.content)",
        "    patches = []",
        "    // Merge superficial top-level / profundo en sub-objetos. Construir patches.",
        "    for (k in input.campos) {",
        "      if (typeof input.campos[k] === 'object' && input.campos[k] !== null && !Array.isArray(input.campos[k]) && typeof config[k] === 'object') {",
        "        merged = {}",
        "        for (subk in (config[k] || {})) { merged[subk] = config[k][subk] }",
        "        for (subk in input.campos[k]) { merged[subk] = input.campos[k][subk] }",
        "        patches.push({ op: 'replace', path: '/' + k, value: merged })",
        "        config[k] = merged",
        "      } else {",
        "        patches.push({ op: 'replace', path: '/' + k, value: input.campos[k] })",
        "        config[k] = input.campos[k]",
        "      }",
        "    }",
        "    patches.push({ op: 'replace', path: '/_updated_at', value: now })",
        "    config._updated_at = now",
        "    edit_resp = await publishAndWait('fs.edit.request', {",
        "      project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json',",
        "      patches: patches",
        "    })",
        "    if (edit_resp.status >= 400) return edit_resp",
        "  }",
        "",
        "  // Garantizar correlation_id no-vacio --uuid() si input no trae",
        "  correlation_id_efectivo = input.correlation_id || uuid()",
        "  await publish('cartadigital.config.actualizada', { project_id: input.project_id, campos_modificados: Object.keys(input.campos), correlation_id: correlation_id_efectivo, timestamp: nowISO() })",
        "  return { status: 200, data: config }"
      ],
      "reglas_clave": [
        "Merge profundo en sub-objetos (branding, contacto). Si el campo es array o primitivo, reemplaza.",
        "Rama dual: 404 → fs.write atomico del store inicial; existe → fs.edit con patches por cada campo modificado + /_updated_at.",
        "El loop de merge usa for-in explicito — sin spread ni Object.assign (pseudocodigo-estilo.contract)."
      ],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE"]
    },

    "get_carta_publica": {
      "input": "{ project_id (req), request_id (req), correlation_id? }",
      "pseudocodigo": [
        "async get_carta_publica(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json' })",
        "  if (raw.status === 404) return RESOURCE_NOT_FOUND { entity_type: 'carta_publica', hint: 'aun no hay carta compuesta — el LLM debe delegar a cartadigital-composer primero' }",
        "  if (raw.status >= 400) return raw",
        "  config = JSON.parse(raw.content)",
        "  if (!config.carta_compuesta) return RESOURCE_NOT_FOUND { entity_type: 'carta_publica', hint: 'config existe pero carta_compuesta es null — delegar a composer' }",
        "  return { status: 200, data: { branding: config.branding, contacto: config.contacto, carta: config.carta_compuesta } }"
      ],
      "errores_posibles": ["INVALID_INPUT", "RESOURCE_NOT_FOUND", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE"]
    },

    "set_carta_compuesta": {
      "input": "{ project_id (req), request_id (req), correlation_id?, user_id?, carta_compuesta (req — objeto producido por cartadigital-composer), generado_por? (string id de agente) }",
      "pseudocodigo": [
        "async set_carta_compuesta(input):",
        "  if (!input.project_id) return INVALID_INPUT { field: 'project_id' }",
        "  if (!input.carta_compuesta || typeof input.carta_compuesta !== 'object') return INVALID_INPUT { field: 'carta_compuesta' }",
        "",
        "  raw = await publishAndWait('fs.read.request', { project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json' })",
        "  if (raw.status >= 400 && raw.status !== 404) return raw",
        "  now = nowISO()",
        "",
        "  // Construir carta_compuesta entera copiando campo a campo (sin spread — pseudocodigo-estilo.contract).",
        "  // El agente compositor genera la carta completa — esta operacion la sustituye entera, no hace merge.",
        "  carta_nueva = {}",
        "  for (campo in input.carta_compuesta) { carta_nueva[campo] = input.carta_compuesta[campo] }",
        "  carta_nueva.generado_at = now",
        "  carta_nueva.generado_por = input.generado_por || 'unknown'",
        "",
        "  //    Rama A — archivo NO existe: fs.write atomico del store inicial con la carta_compuesta.",
        "  //    Rama B — archivo existe: fs.edit declarativo (RFC 6902). Patron UPDATE 1-campo:",
        "  //      op:replace /carta_compuesta (sustitucion entera) + op:replace /_updated_at.",
        "  //      No compone el archivo entero.",
        "  if (raw.status === 404) {",
        "    config = { _version: '1.0', _updated_at: now, branding: {}, opciones_visualizacion: {}, carta_compuesta: carta_nueva }",
        "    write_resp = await publishAndWait('fs.write.request', {",
        "      project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json',",
        "      content: JSON.stringify(config, null, 2), encoding: 'utf-8', atomic: true",
        "    })",
        "    if (write_resp.status >= 400) return write_resp",
        "  } else {",
        "    edit_resp = await publishAndWait('fs.edit.request', {",
        "      project_id: input.project_id, path: '/storage/pizzepos/carta-digital/config.json',",
        "      patches: [",
        "        { op: 'replace', path: '/carta_compuesta', value: carta_nueva },",
        "        { op: 'replace', path: '/_updated_at',     value: now }",
        "      ]",
        "    })",
        "    if (edit_resp.status >= 400) return edit_resp",
        "  }",
        "",
        "  // Garantizar correlation_id no-vacio --uuid() si input no trae",
        "  correlation_id_efectivo = input.correlation_id || uuid()",
        "  await publish('cartadigital.carta_publica.actualizada', { project_id: input.project_id, generado_at: carta_nueva.generado_at, generado_por: carta_nueva.generado_por, correlation_id: correlation_id_efectivo, timestamp: nowISO() })",
        "  return { status: 200, data: carta_nueva }"
      ],
      "reglas_clave": [
        "Reemplaza la carta_compuesta entera (no merge — el agente compositor genera completa).",
        "Anyade generado_at + generado_por automaticamente copiando campo a campo — sin spread (pseudocodigo-estilo.contract).",
        "Rama dual: 404 → fs.write atomico del store inicial; existe → fs.edit con op:replace /carta_compuesta + op:replace /_updated_at."
      ],
      "errores_posibles": ["INVALID_INPUT", "UPSTREAM_TIMEOUT", "UPSTREAM_UNREACHABLE"]
    }
  },

  "trabajo_pendiente": [
    {
      "id": "subscribers_carta_actualizada_y_tarifas_config",
      "estado": "abierto",
      "descripcion": "El legacy se subscribia a carta.actualizada y tarifas.config.actualizada para invalidar la carta_compuesta cuando los datos upstream cambiaban. El blueprint no es reactivo. Posible solucion: marcar la carta_compuesta como 'desfasada' en update_config cuando llegan ciertos eventos via agente observer, o que el LLM al entrar a page=carta-digital verifique fechas (carta.meta.updated_at vs carta_compuesta.generado_at) y proponga recomponer."
    },
    {
      "id": "schemas_oficiales_carta_digital",
      "estado": "abierto",
      "descripcion": "Eventos cartadigital.config.actualizada y cartadigital.carta_publica.actualizada no tienen schemas oficiales. Crear cuando el subsistema-carta tenga sus schemas formales."
    }
  ]
}
