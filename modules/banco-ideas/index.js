{
  "index.js": "'use strict';\n\nconst ModuloHibridoReflejo = require('../../_shared/modulo-hibrido-reflejo');\nconst PosPersistencia = require('../../_shared/pos-persistencia');\nconst { puntaje, minPorPuntaje } = require('../../_shared/prioridad');\n\nconst TOPE = 100;\n\nclass BancoIdeas extends ModuloHibridoReflejo {\n  constructor(opciones) {\n    super(opciones);\n    this.name = 'banco-ideas';\n    this.version = '0.1.0';\n    this.bus = (opciones && opciones.bus) || null;\n    this.persistencia = new PosPersistencia({\n      archivo: 'data/newsletter/banco-ideas.json',\n      snapshot: true,\n      hidratar: true,\n      onProjectActivated: true\n    });\n    this.ideas = [];\n    this._hidratar();\n  }\n\n  _hidratar() {\n    try {\n      let data = null;\n      if (typeof this.persistencia.hidratar === 'function') data = this.persistencia.hidratar();\n      else if (typeof this.persistencia.cargar === 'function') data = this.persistencia.cargar();\n      if (data && Array.isArray(data.ideas)) this.ideas = data.ideas;\n    } catch (err) {\n      this.ideas = [];\n    }\n  }\n\n  _persistir() {\n    const datos = { ideas: this.ideas, actualizadoEn: new Date().toISOString() };\n    try {\n      if (typeof this.persistencia.guardar === 'function') {\n        this.persistencia.guardar(datos);\n      } else if (typeof this.persistencia.snapshot === 'function') {\n        this.persistencia.snapshot(datos);\n      }\n    } catch (err) {\n      // best-effort: no romper la operación si la persistencia falla\n    }\n  }\n\n  _emitir(evento, datos) {\n    try {\n      if (this.bus && typeof this.bus.publish === 'function') this.bus.publish(evento, datos);\n      else if (typeof this.publicar === 'function') this.publicar(evento, datos);\n      else if (typeof this.emit === 'function') this.emit(evento, datos);\n    } catch (err) {\n      // no propagar errores de eventos\n    }\n  }\n\n  _responder(eventoRequest, requestId, status, data) {\n    const nombreRespuesta = eventoRequest.replace('.request', '.response');\n    const payload = { request_id: requestId, status, data };\n    if (this._respuestaActual) {\n      this._respuestaActual(payload);\n    } else {\n      this._emitir(nombreRespuesta, payload);\n    }\n  }\n\n  _atender(evento, contexto, respuesta, siguiente) {\n    this._respuestaActual = typeof respuesta === 'function' ? respuesta : null;\n    const ctx = contexto || {};\n    const requestId = ctx.request_id || ctx.requestId || null;\n\n    try {\n      switch (evento) {\n        case 'newsletter.idea.registrar.request':\n          this.onRegistrarRequest(ctx, requestId, respuesta);\n          break;\n        case 'newsletter.idea.aparcar.request':\n          this.onAparcarRequest(ctx, requestId, respuesta);\n          break;\n        case 'newsletter.banco.listar.request':\n          this.onListarRequest(ctx, requestId, respuesta);\n          break;\n        case 'newsletter.banco.seleccionar.request':\n          this.onSeleccionarRequest(ctx, requestId, respuesta);\n          break;\n        case 'project.activated':\n          this.onProjectActivated(ctx);\n          if (typeof siguiente === 'function') siguiente();\n          break;\n        default:\n          if (typeof siguiente === 'function') siguiente();\n      }\n    } catch (err) {\n      this._responder(evento, requestId, 'error', { error: err.message });\n    } finally {\n      this._respuestaActual = null;\n    }\n  }\n\n  onRegistrarRequest(ctx, requestId, respuesta) {\n    this._registrar(ctx, requestId);\n  }\n\n  onAparcarRequest(ctx, requestId, respuesta) {\n    this._aparcar(ctx, requestId);\n  }\n\n  onListarRequest(ctx, requestId, respuesta) {\n    this._listar(ctx, requestId);\n  }\n\n  onSeleccionarRequest(ctx, requestId, respuesta) {\n    this._seleccionar(ctx, requestId);\n  }\n\n  onProjectActivated() {\n    this._hidratar();\n  }\n\n  _registrar(ctx, requestId) {\n    const entrada = ctx.idea && typeof ctx.idea === 'object' ? ctx.idea : ctx;\n    const id = ctx.id || entrada.id || 'idea_' + Date.now() + '_' + Math.random().toString(36).slice(2, 8);\n    const ahora = new Date().toISOString();\n\n    const idea = {\n      id: id,\n      titulo: entrada.titulo || entrada.nombre || 'Sin título',\n      descripcion: entrada.descripcion || '',\n      estado: 'Cruda',\n      valor: Number.isFinite(Number(entrada.valor)) ? Number(entrada.valor) : 0,\n      probabilidadEvidencia: Number.isFinite(Number(entrada.probabilidadEvidencia)) ? Number(entrada.probabilidadEvidencia) : 0,\n      esfuerzo: Number.isFinite(Number(entrada.esfuerzo)) && Number(entrada.esfuerzo) > 0 ? Number(entrada.esfuerzo) : 1,\n      prioridadCalculada: 0,\n      fechaRegistro: ahora,\n      decision: 0\n    };\n    idea.prioridadCalculada = puntaje(idea);\n\n    this.ideas.push(idea);\n    let descartada = null;\n    if (this.ideas.length > TOPE) {\n      const min = minPorPuntaje(this.ideas);\n      if (min.idea) {\n        descartada = min.idea;\n        this.ideas = this.ideas.filter(i => i.id !== min.idea.id);\n      }\n    }\n\n    this._persistir();\n    this._emitir('newsletter.idea.registrada', { idea: idea });\n    if (descartada) this._emitir('newsletter.banco.tope_alcanzado', { tope: TOPE, descartada: descartada });\n\n    this._responder('newsletter.idea.registrar.request', requestId, 'ok', {\n      idea: idea,\n      descartada: descartada ? { id: descartada.id, titulo: descartada.titulo, prioridadCalculada: descartada.prioridadCalculada } : null,\n      total: this.ideas.length,\n      tope: TOPE\n    });\n  }\n\n  _aparcar(ctx, requestId) {\n    const id = ctx.id || (ctx.idea && ctx.idea.id);\n    const idea = this._buscarIdea(id);\n    if (!idea) {\n      this._responder('newsletter.idea.aparcar.request', requestId, 'error', { error: 'idea_no_encontrada', id: id });\n      return;\n    }\n    idea.estado = 'Aparcada';\n    idea.decision = 10;\n    idea.motivo = 'PendienteDeEvidencia';\n    idea.fechaAparcado = new Date().toISOString();\n\n    this._persistir();\n    this._emitir('newsletter.idea.aparcada', { idea: idea });\n\n    this._responder('newsletter.idea.aparcar.request', requestId, 'ok', { idea: idea });\n  }\n\n  _listar(ctx, requestId) {\n    const estado = ctx.estado || null;\n    const ideas = estado\n      ? this.ideas.filter(i => i.estado === estado)\n      : this.ideas.slice();\n    ideas.sort((a, b) => b.prioridadCalculada - a.prioridadCalculada);\n\n    this._responder('newsletter.banco.listar.request', requestId, 'ok', {\n      total: ideas.length,\n      ideas: ideas\n    });\n  }\n\n  _seleccionar(ctx, requestId) {\n    const id = ctx.id || (ctx.idea && ctx.idea.id);\n    const idea = this._buscarIdea(id);\n    if (!idea) {\n      this._responder('newsletter.banco.seleccionar.request', requestId, 'error', { error: 'idea_no_encontrada', id: id });\n      return;\n    }\n    if (idea.estado !== 'Seleccionada') {\n      idea.estado = 'Seleccionada';\n      idea.fechaSeleccion = new Date().toISOString();\n      idea.decision = 1;\n      this._persistir();\n    }\n    this._responder('newsletter.banco.seleccionar.request', requestId, 'ok', { idea: idea });\n  }\n\n  _buscarIdea(id) {\n    return this.ideas.find(i => i.id === id) || null;\n  }\n}\n\nmodule.exports = BancoIdeas;\n",
  "module.json": "{\n  \"_doc\": \"Banco de ideas del newsletter. REFLEJO CUSTODIO: single-writer de data/newsletter/banco-ideas.json. Registra ideas crudas, aplica tope 100 descartando la de menor prioridad (puntaje = valor × probabilidadEvidencia / esfuerzo), aparca ideas PendienteDeEvidencia, lista por estado y selecciona una idea para el próximo newsletter.\",\n  \"name\": \"banco-ideas\",\n  \"version\": \"0.1.0\",\n  \"description\": \"Banco de ideas del newsletter con tope 100 y priorización por valor×probabilidad/ esfuerzo.\",\n  \"language\": \"es\",\n  \"subscribes\": {\n    \"newsletter.idea.registrar.request\": \"onRegistrarRequest\",\n    \"newsletter.idea.aparcar.request\": \"onAparcarRequest\",\n    \"newsletter.banco.listar.request\": \"onListarRequest\",\n    \"newsletter.banco.seleccionar.request\": \"onSeleccionarRequest\",\n    \"project.activated\": \"onProjectActivated\"\n  },\n  \"publishes\": {\n    \"newsletter.idea.registrada\": \"idea registrada en el banco\",\n    \"newsletter.idea.aparcada\": \"idea marcada como PendienteDeEvidencia\",\n    \"newsletter.banco.tope_alcanzado\": \"se descartó la de menor puntaje al superar el tope 100\",\n    \"newsletter.idea.registrar.response\": \"respuesta a registrar\",\n    \"newsletter.idea.aparcar.response\": \"respuesta a aparcar\",\n    \"newsletter.banco.listar.response\": \"respuesta a listar\",\n    \"newsletter.banco.seleccionar.response\": \"respuesta a seleccionar\"\n  },\n  \"persistencia\": {\n    \"archivo\": \"data/newsletter/banco-ideas.json\",\n    \"snapshot\": true,\n    \"hidratar\": true,\n    \"onProjectActivated\": true\n  }\n}",
  "_shared/prioridad.js": "'use strict';\n\nfunction _numero(valor, defecto) {\n  const n = Number(valor);\n  return Number.isFinite(n) ? n : (defecto !== undefined ? defecto : 0);\n}\n\nfunction puntaje(idea) {\n  if (!idea || typeof idea !== 'object') return 0;\n  const valor = _numero(idea.valor ?? (idea.puntaje && idea.puntaje.valor));\n  const probabilidadEvidencia = _numero(idea.probabilidadEvidencia ?? (idea.puntaje && idea.puntaje.probabilidadEvidencia));\n  const esfuerzo = _numero(idea.esfuerzo ?? (idea.puntaje && idea.puntaje.esfuerzo), 1);\n  if (esfuerzo <= 0) return 0;\n  return (valor * probabilidadEvidencia) / esfuerzo;\n}\n\nfunction maxPorPuntaje(ideas) {\n  let mejor = null;\n  let mayor = -Infinity;\n  const lista = Array.isArray(ideas) ? ideas : [];\n  for (const idea of lista) {\n    const p = puntaje(idea);\n    if (p > mayor) {\n      mayor = p;\n      mejor = idea;\n    }\n  }\n  return { idea: mejor, puntajeCalculada: mejor ? mayor : 0 };\n}\n\nfunction minPorPuntaje(ideas) {\n  let peor = null;\n  let menor = Infinity;\n  const lista = Array.isArray(ideas) ? ideas : [];\n  for (const idea of lista) {\n    const p = puntaje(idea);\n    if (p < menor) {\n      menor = p;\n      peor = idea;\n    }\n  }\n  return { idea: peor, puntajeCalculada: peor ? menor : 0 };\n}\n\nmodule.exports = { puntaje: puntaje, maxPorPuntaje: maxPorPuntaje, minPorPuntaje: minPorPuntaje };\n",
  "slug": "newsletter/banco-ideas"
}