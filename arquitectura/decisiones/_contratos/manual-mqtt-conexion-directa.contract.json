{
  "_doc": "Manual operativo de la conexion MQTT directa al bus de Enki. Cubre el protocolo, el patron de topics (publish vs subscribe), el envelope canonico, la correlacion request/response, las secuencias canonicas para operar el sistema sin pasar por helpers, las probes de diagnostico forense que se aplican cuando los helpers de audit-module se quedan cortos, el catalogo de eventos seguros para clientes externos, y los errores clasicos que aparecen al construir clientes ad-hoc. Documento de referencia operativa — sin validator, sin valoraciones, solo procedimientos. Hermano de manual-audit-module-y-helpers.contract.json: si los helpers no bastan, este manual te baja un nivel.",

  "id": "manual-mqtt-conexion-directa",
  "version": "1.0.0",
  "creada": "2026-05-20",
  "tipo": "manual_operativo",

  "cuando_usar_este_manual": {
    "_doc": "Bajar a MQTT directo NO es lo habitual. Los helpers de audit-module cubren el 95% de casos. Recurrir a esto cuando:",
    "casos_legitimos": [
      "Un helper cuelga con timeout silencioso y necesitas saber si el bus respondio (verificar que llegaron las responses correlacionadas).",
      "Necesitas verificar persistencia en disco tras una mutacion que el LLM dice haber hecho (probar fs.read.request directo).",
      "Necesitas inspeccionar eventos del dominio en vivo (escuchar receta.* / agent.execute.* / chat.* mientras el sistema corre).",
      "Necesitas reproducir un bug del runtime sin pasar por el chat-flow (probar fs.write.request con un payload conocido para descartar bug del filesystem).",
      "Estas escribiendo un helper nuevo y necesitas confirmar los topic patterns y el envelope.",
      "Necesitas listar / crear / activar proyectos desde un entorno donde no hay frontend (CI, sesion remota efimera, etc.)."
    ],
    "casos_no_legitimos": [
      "Reemplazar el chat-flow por bot ad-hoc para 'ahorrar tiempo'. El chat-flow lleva disciplina de companero-viaje (correlation_id, memorias enchufables, prompt-builder) que un cliente directo no lleva.",
      "Modificar estado de produccion sin pasar por el flujo del usuario. El bus es read-only-safe pero no write-safe para outsiders — algunos eventos (project.delete, db.query.request con SQL libre) son destructivos sin guardrails.",
      "Mantener una conexion persistente como subscriber permanente. Los helpers usan reconnectPeriod=0 y se cierran tras la respuesta — clientes long-lived deben ser modulos del sistema, no scripts ad-hoc."
    ]
  },

  "datos_de_conexion": {
    "url": "wss://enki-ai.online/mqtt",
    "override_env_var": "AUDIT_BROKER (todos los helpers de scripts/audit-helpers/ la respetan)",
    "protocolo": "WebSocket sobre MQTT v3.1.1",
    "libreria_cliente_recomendada": "mqtt.js (compatible nativa) — npm install mqtt",
    "auth_mqtt": "ninguna (broker abierto para clientes con la URL conocida)",
    "auth_http_para_export": "Bearer token en header Authorization (env EXPORT_TOKEN, fallback hardcoded 'nonina'). Solo aplica a la API HTTP de conversation-export, NO a MQTT.",
    "client_id": "string unico cualquiera por conexion. Convencion: '<prefijo-corto>-<timestamp>' (ej. 'audit-1748123456789'). Reutilizar client_id entre clientes activos provoca desconexion del anterior.",
    "opciones_recomendadas_mqtt_connect": {
      "clientId": "string unico",
      "connectTimeout": 8000,
      "reconnectPeriod": 0,
      "_nota_reconnect": "Scripts efimeros (audit, probes) DEBEN usar reconnectPeriod=0 para que el script termine cuando el broker se cierra. Solo modulos permanentes del sistema usan reconnect."
    }
  },

  "convencion_de_topics": {
    "_doc": "Hay DOS familias de topics. La mayoria del sistema usa la familia 'eventbus canonica' (core/*/events/...). Una minoria de operaciones del UI usa topics directos (ui/request/..., ui/response/...) que NO siguen el patron eventbus.",

    "familia_eventbus_canonica": {
      "topic_para_publish": "core/*/events/<event_type_with_slashes>",
      "topic_para_subscribe": "core/+/events/<event_type_with_slashes>",
      "razon_de_los_dos_patrones": "El asterisco '*' es un segmento LITERAL en el topic de publish (significa 'a todos los cores'). El '+' es el wildcard MQTT estandar que matchea cualquier valor del segmento (incluyendo el '*' literal). Por eso publish y subscribe difieren — el cliente publica 'a todos' y se suscribe 'de cualquiera'.",
      "conversion_event_type_a_topic": {
        "regla": "Los puntos del event_type se convierten a slashes, EXCEPTO que se mantienen agrupados los segmentos finales si el verbo es compuesto. En la practica: split por '.', primer segmento sin cambio, resto unido con '/' tras el primer punto.",
        "implementacion_de_referencia": "const buildTopic = (et) => { const p = et.split('.'); return 'core/*/events/' + p[0] + (p.length > 1 ? '/' + p.slice(1).join('/') : ''); }",
        "ejemplos": {
          "chat.message.saved": "core/*/events/chat/message/saved",
          "fs.read.request": "core/*/events/fs/read/request",
          "fs.read.response": "core/*/events/fs/read/response",
          "receta.creada": "core/*/events/receta/creada",
          "receta.estado.actualizada": "core/*/events/receta/estado/actualizada",
          "agent.execute.request": "core/*/events/agent/execute/request",
          "ingrediente.precio.actualizado": "core/*/events/ingrediente/precio/actualizado"
        }
      },
      "subscribir_a_familias_enteras": {
        "_doc": "Usar wildcard '+' para escuchar todo un namespace.",
        "ejemplos": {
          "todos_los_eventos_de_fs": "core/+/events/fs/+/+ (cubre fs.read.request, fs.read.response, fs.write.request, fs.write.response, fs.file.created, etc.)",
          "todos_los_eventos_de_recetas": "core/+/events/receta/+ y core/+/events/receta/+/+ (uno-segmento y dos-segmentos)",
          "todo_lo_relacionado_con_agentes": "core/+/events/agent/+/+ (agent.execute.request, .response, .progress, .failed)"
        }
      }
    },

    "familia_ui_directa": {
      "_doc": "Para iniciar acciones desde el UI. NO siguen el patron eventbus — son topics directos con par request/response acoplados por request_id en el topic.",
      "topic_request": "ui/request/<dominio>/<accion>",
      "topic_response": "ui/response/<request_id_uuid>",
      "ejemplos_canonicos": {
        "crear_conversacion": {
          "publish": "ui/request/conversation/create",
          "subscribe": "ui/response/<request_id>",
          "shape_payload": "{ request_id, data: { project_id, title, user_id } }"
        },
        "enviar_mensaje_a_chat": {
          "publish": "ui/request/conversation/send",
          "subscribe_para_respuesta_asincrona": "core/+/events/chat/assistant/saved y core/+/events/ai/chat/failed (esto SI es eventbus canonico — la respuesta del LLM tarda y se publica como evento, no como response UI).",
          "shape_payload": "{ request_id, data: { project_id, conversation_id, message, user_id, channel, page_id, settings: { provider?, model?, thinking? } } }"
        }
      },
      "razon_de_existir_aparte": "El UI necesita un canal sincronico request/response por uuid sin depender del subscribe global del eventbus. Topics dedicados por request_id evitan el filtering que harias en eventbus.",
      "no_inventarse_topics_ui": "Solo conversation/create y conversation/send estan documentados. Otros dominios del UI (project, prompt, etc.) van por eventbus canonico (project.create.request, etc.)."
    }
  },

  "envelope_canonico": {
    "_doc": "Estructura JSON del payload MQTT para topics de la familia eventbus. Los topics ui/request/* tienen shape diferente (mas plano, ver mas abajo).",

    "shape_eventbus": {
      "event_id": "uuid v4 — identificador unico del evento (no es el request_id; un mismo request_id puede tener N event_id en su lifecycle).",
      "event_type": "string canonico — '<modulo>.<entidad>.<verbo>' o '<dominio>.<accion>.<request|response|failed>'. Debe coincidir con el topic (modulo si conversion_event_type_a_topic).",
      "timestamp": "ISO 8601 — momento de publicacion.",
      "source": {
        "_doc": "Identifica quien publica. Util para tracing.",
        "core_id": "string identificando el proceso emisor (ej. 'core-a', 'audit-helper', 'core-id-del-modulo')",
        "module_id": "string opcional con el slug del modulo emisor (ej. 'recetas', 'chat-io')"
      },
      "data": {
        "_doc": "Payload del evento. Aqui va el request_id y los campos de dominio.",
        "request_id": "uuid v4 — REQUERIDO en cualquier par request/response. El emisor lo genera; el responder lo copia en su response.",
        "correlation_id": "uuid opcional — propagacion causal a traves de cadenas de eventos. Se mantiene invariante por una accion de usuario completa.",
        "...campos_de_dominio": "specificos del event_type (ej. para fs.write.request: { project_id, path, content, encoding, atomic })"
      },
      "metadata": "objeto vacio {} o con campos auxiliares de trazabilidad. NO meter dominio aqui — va en data."
    },

    "shape_ui_request": {
      "_doc": "Topics ui/request/* usan shape mas plano. NO tienen event_id/event_type/timestamp/source/metadata.",
      "request_id": "uuid",
      "data": "{ ...payload de la accion }"
    },

    "implementacion_de_referencia_envelope": "const env = (et, d) => ({ event_id: crypto.randomUUID(), event_type: et, timestamp: new Date().toISOString(), source: { core_id: 'audit-helper' }, data: d, metadata: {} });"
  },

  "correlacion_request_response": {
    "_doc": "Es la mecanica universal para parejas request/response sobre MQTT. Sin correlacion, los clientes no pueden distinguir su response de otras en vuelo.",

    "principio": "El cliente genera un request_id UUID antes de publicar y lo incluye en data.request_id. El responder copia ese mismo request_id en data.request_id de su response. El cliente filtra los mensajes entrantes por d.request_id === <su_uuid>.",

    "patron_canonico_de_implementacion": [
      "1. Generar r = crypto.randomUUID() ANTES de subscribir.",
      "2. Conectar al broker y esperar evento 'connect'.",
      "3. Subscribir al topic de response (await que el callback de subscribe complete antes de publicar).",
      "4. Publicar el request_id con data.request_id = r.",
      "5. En el handler 'message', JSON.parse el payload y si data.request_id === r → resolver. Si no → ignorar (es de otro cliente o de una request anterior).",
      "6. Timeout de seguridad (15-30s tipicos) para evitar hangs eternos."
    ],

    "errores_de_correlacion_que_aparecen_facil": [
      "Generar el request_id DESPUES de publicar (race: la response llega antes de que filtres por uuid).",
      "Publicar ANTES de que el subscribe se haya completado (race: pierdes la response porque la subscripcion no estaba activa). Solucion: usar el callback (err, granted) => publish(...) del subscribe.",
      "Comparar request_id con != vs !== en JS (los uuids son strings, == bastaria, pero usar === por consistencia).",
      "Olvidar el setTimeout de seguridad y dejar el script colgado para siempre cuando el responder esta caido."
    ]
  },

  "secuencias_canonicas": {
    "_doc": "Las 4 secuencias que aparecen casi siempre en clientes ad-hoc. Implementaciones de referencia.",

    "listar_proyectos": {
      "objetivo": "Resolver nombre → uuid de proyecto, o inventariar lo que hay.",
      "publish": "core/*/events/project/list/request con data: { request_id }",
      "subscribe": "core/+/events/project/list/response",
      "filter": "d.request_id === r",
      "response_shape": "data: { request_id, projects: [{ id, name, base_path, description, metadata, created_at, ... }] }",
      "comportamiento_si_no_hay_proyectos": "Devuelve projects: [] (array vacio). No es error."
    },

    "crear_conversacion": {
      "objetivo": "Crear una conv para enviar mensajes despues.",
      "publish": "ui/request/conversation/create  (TOPIC DIRECTO, no eventbus)",
      "shape_payload": "{ request_id, data: { project_id, title?, user_id? } }",
      "subscribe": "ui/response/<request_id>  (TOPIC DIRECTO, parametrizado por request_id)",
      "response_shape": "{ request_id, data: { conversation_id, ...resto } }",
      "alternativa_via_helper": "node scripts/audit-helpers/create-conversation.js <project_id|name> [title] — imprime solo el conversation_id"
    },

    "enviar_mensaje_y_esperar_respuesta": {
      "objetivo": "Mandar un mensaje al chat y capturar la respuesta del asistente.",
      "publish": "ui/request/conversation/send  (TOPIC DIRECTO)",
      "shape_payload": "{ request_id, data: { project_id, conversation_id, message, user_id, channel: 'web', page_id, settings: { provider?, model?, thinking? } } }",
      "subscribes_necesarios_para_capturar_respuesta": [
        "core/+/events/chat/assistant/saved  (la respuesta del LLM persistida — formato eventbus canonico)",
        "core/+/events/ai/chat/failed  (par failure correlacionado por conversation_id)"
      ],
      "filter_para_la_respuesta": "d.conversation_id === <conv_uuid> && (event_type === 'chat.assistant.saved' || event_type === 'ai.chat.failed')",
      "nota_de_filtrado_de_eco": "chat-io reemite el mensaje con su propio source.module_id='chat-io'. Filtrar por p.source?.module_id !== 'chat-io' para capturar solo la final del asistente.",
      "timeout_recomendado": "60-180s — los flujos con tool calls pueden iterar varias veces.",
      "alternativa_via_helper": "node scripts/audit-helpers/send-message.js <project> <conv> <page_id> 'msg' [wait_ms] [--provider] [--model] [--thinking]"
    },

    "publicar_evento_de_dominio_y_esperar_response": {
      "objetivo": "Patron generico request/response sobre eventbus (no UI).",
      "ejemplo_fs_read": {
        "publish": "core/*/events/fs/read/request con data: { request_id, project_id, path }",
        "subscribe": "core/+/events/fs/read/response",
        "response_shape": "data: { request_id, content, path, ... }  // content es el archivo serializado segun encoding"
      },
      "ejemplo_db_query": {
        "publish": "core/*/events/db/query/request con data: { request_id, project_id, query, params, read_only }",
        "subscribe": "core/+/events/db/query/response",
        "_advertencia": "db.query.request acepta SQL libre. NO usar con queries de escritura desde clientes ad-hoc en proyectos de produccion."
      }
    }
  },

  "probes_de_diagnostico_forense": {
    "_doc": "Patrones aplicados en audits reales cuando los helpers se quedan cortos. Cada uno responde a una pregunta de diagnostico concreta.",

    "probe_1_verificar_persistencia_tras_timeout_silencioso": {
      "pregunta": "El LLM dijo haber creado/actualizado una receta pero el chat timed-out. ¿Llego el cambio a disco?",
      "metodo": "fs.read.request al path canonico del modulo (/recetas.json para recetas, /escandallos.json para escandallo, etc.). Parsear content. Comparar updated_at, version, presencia de la entidad nueva.",
      "snippet_de_referencia": "publish fs.read.request con {project_id, path: '/recetas.json'}, espera fs.read.response, JSON.parse(d.content), iterar j.recetas y verificar.",
      "interpretacion": "Si la entidad esta en disco pero el chat no respondio: bug en el chat-flow (no en el modulo ni en filesystem). Si la entidad NO esta en disco: el LLM no llego a publicar fs.write.request (bug del runtime LLM) o publico mal (revisar tool_calls en messages_raw[].metadata)."
    },

    "probe_2_aislar_si_filesystem_es_el_culpable": {
      "pregunta": "Las mutaciones cuelgan via chat. ¿Es el filesystem el que esta roto o es el LLM que llama mal?",
      "metodo": "Publicar fs.write.request directo al bus con payload conocido bueno (path + content valido). Esperar response. Si responde con created:true → filesystem OK, el bug esta en como el LLM construye el payload. Si timeout → filesystem caido.",
      "snippet_de_referencia": "publish fs.write.request con {project_id, path: '/_audit-probe.json', content: '{...}', encoding: 'utf-8', atomic: true}, espera fs.write.response, verificar created.",
      "advertencia": "Usar paths con prefijo `_audit-` o `_probe-` para distinguir de archivos del dominio. Limpiar despues si el proyecto esta vivo."
    },

    "probe_3_inspeccionar_tool_calls_del_llm": {
      "pregunta": "El LLM dice X pero no lo hizo. ¿Que tool calls emitio realmente?",
      "metodo": "Tras un export, leer messages_raw[i].metadata como JSON. Tiene { provider, model, iterations, tool_calls: [{ name, args, status, result }] }. Cada tool_call tiene los args literales que el LLM envio.",
      "como_detectar_payload_vacio": "Inspeccionar tool_calls[].args. Si es bus.publishAndWait, ver si args.payload existe y tiene los campos esperados. Bug observado en audit recetas 2026-05-20: anthropic publicaba fs.write.request con args = { event: 'fs.write.request' } y payload omitido entero.",
      "como_detectar_alucinacion": "Si tool_calls esta vacio (tools: []) y el assistant_message contiene afirmaciones sobre estado del dominio, comparar con fs.read.request directo. Si difieren, el LLM se invento la respuesta del contexto."
    },

    "probe_4_listening_en_vivo_de_un_subsistema": {
      "pregunta": "¿Que eventos del subsistema X estan ocurriendo mientras el usuario opera?",
      "metodo": "Conectar y subscribir broadly al namespace (ej. 'core/+/events/receta/+', 'core/+/events/agent/+/+'). Hacer la operacion en el chat. Capturar el stream cronologico.",
      "uso_tipico": "Verificar que un evento canonico se publica (ej. receta.creada tras crear); detectar eventos huerfanos sin handler; medir latencia entre request y response."
    },

    "probe_5_verificar_si_un_agente_se_invoca": {
      "pregunta": "Pedi al LLM que delegara al agente X. ¿Se invoco realmente?",
      "metodo": "Subscribir a core/+/events/agent/execute/+ ANTES de mandar el mensaje. Ver si llega un agent.execute.request con agent_name === <esperado>. Si no llega: el LLM no supo delegar (bug de prompt/catalogo) o el agente no esta en scope.",
      "diferenciar_de_chat_normal": "Si solo hay tool_calls de tipo bus.publishAndWait → fs.read.request, el LLM resolvio el problema sin agentes. Eso puede ser correcto (lectura simple) o un finding (deberia haber delegado para razonar)."
    }
  },

  "eventos_seguros_para_clientes_externos": {
    "_doc": "Catalogo de eventos que es razonable invocar desde un script ad-hoc para auditar o diagnosticar. NO es la API publica del sistema — es la lista de eventos que en la practica no rompen nada cuando los usas read-only.",

    "read_only_seguros": {
      "fs.read.request": "Leer cualquier archivo del proyecto via path con leading-slash. SAFE: no muta.",
      "fs.list.request": "Listar contenido de un directorio del proyecto. SAFE: no muta.",
      "db.query.request_con_read_only_true": "Ejecutar SQL con read_only: true en el payload. SAFE: rechazado si la query contiene INSERT/UPDATE/DELETE.",
      "project.list.request": "Inventario de proyectos. SAFE: no muta.",
      "project.get.request": "Detalles de un proyecto por id. SAFE: no muta.",
      "project.active.request": "Lista de proyectos activos. SAFE: no muta."
    },

    "mutaciones_aceptables_para_probes": {
      "_doc": "Mutaciones acotadas que un probe puede ejecutar siempre que use paths/keys claramente de prueba.",
      "fs.write.request_con_path_de_prueba": "Solo si el path empieza por '/_audit-' o '/_probe-'. NUNCA tocar /recetas.json, /escandallos.json, /tarifas.json, /carta-*.json, etc. directamente con un cliente externo."
    },

    "mutaciones_destructivas_evitar_desde_clientes_externos": [
      "project.delete.request (borra proyecto entero)",
      "project.update.request (modifica metadatos de proyectos en uso)",
      "db.query.request con SQL libre de mutacion (puede dejar la DB en estado inconsistente)",
      "fs.write.request a paths del dominio (sobreescribe estado canonico — debe ir por el modulo correspondiente)",
      "credential.write.request / credential.delete.request (rompe acceso a providers)",
      "Cualquier <modulo>.estado.actualizar.request sin pasar por la regla de transiciones del modulo (rompe history y estado_operativo)"
    ]
  },

  "errores_clasicos_de_clientes_ad_hoc": {
    "_doc": "Bugs frecuentes al construir un cliente MQTT directo. Aparecidos en audits reales.",

    "subscribe_publish_race": {
      "sintoma": "El publish funciona y el servidor responde, pero el cliente nunca recibe la response.",
      "causa": "El publish se ejecuto antes de que el subscribe estuviera completamente registrado en el broker. El response llego antes de que el cliente fuera subscriber.",
      "solucion": "Pasar callback al subscribe: c.subscribe(topic, (err, granted) => c.publish(topic2, payload)). El publish solo ocurre dentro del callback."
    },

    "client_id_duplicado": {
      "sintoma": "El cliente se conecta y se desconecta inmediatamente sin recibir mensajes.",
      "causa": "Otro cliente activo esta usando el mismo clientId. El broker desconecta al anterior.",
      "solucion": "Generar clientId unico cada vez: 'mi-prefijo-' + Date.now() o 'mi-prefijo-' + crypto.randomUUID().slice(0, 8)."
    },

    "olvidar_setTimeout_de_seguridad": {
      "sintoma": "El script cuelga indefinidamente si el bus no responde.",
      "solucion": "Siempre incluir: setTimeout(() => { console.error('TIMEOUT'); process.exit(2); }, 15000) en probes one-shot."
    },

    "no_filtrar_request_id": {
      "sintoma": "El cliente resuelve con la response de otro cliente o de una request previa.",
      "solucion": "Comparar siempre d.request_id === r dentro del handler 'message'. Si no matchea, return; (ignorar)."
    },

    "JSON_parse_sin_try_catch": {
      "sintoma": "El cliente crashea al recibir un mensaje no-JSON (raro pero posible si llega un retained vacio).",
      "solucion": "try { msg = JSON.parse(m.toString()); } catch (e) { return; }"
    },

    "subscribir_solo_a_response_sin_pensar_en_failed": {
      "sintoma": "El cliente espera fs.read.response pero el modulo respondio con fs.read.failed (no hay tal evento canonico — ejemplo conceptual: ai.chat.response vs ai.chat.failed).",
      "solucion": "Para pares request/response del sistema con par success/failure separado (ver no_silent_failures en CLAUDE.md), subscribir SIEMPRE a ambos: tanto <accion>.response como <accion>.failed correlacionados por request_id."
    },

    "asumir_que_el_payload_completo_esta_en_el_timeline_del_export": {
      "sintoma": "Tras un export, no encuentro los args con los que se publico un evento. Los internal_log del export solo tienen event_type y event_id.",
      "causa": "El timeline del conversation-export NO incluye el payload completo de cada evento del bus — solo metadatos minimos.",
      "solucion": "Para inspeccionar args reales del LLM, leer messages_raw[i].metadata (JSON con tool_calls). Para inspeccionar payloads del bus de dominio, hay que capturarlos en vivo (probe_4) o reproducir el flujo y subscribir broadly."
    }
  },

  "implementacion_de_referencia_minima": {
    "_doc": "Snippet completo Node.js para hacer un probe request/response. Pegable.",
    "snippet": [
      "const mqtt = require('mqtt');",
      "const crypto = require('crypto');",
      "const BROKER = process.env.AUDIT_BROKER || 'wss://enki-ai.online/mqtt';",
      "const buildTopic = (et) => { const p = et.split('.'); return 'core/*/events/' + p[0] + (p.length > 1 ? '/' + p.slice(1).join('/') : ''); };",
      "const subTopic   = (et) => { const p = et.split('.'); return 'core/+/events/' + p[0] + (p.length > 1 ? '/' + p.slice(1).join('/') : ''); };",
      "const env = (et, d) => ({ event_id: crypto.randomUUID(), event_type: et, timestamp: new Date().toISOString(), source: { core_id: 'audit-probe' }, data: d, metadata: {} });",
      "",
      "const c = mqtt.connect(BROKER, { clientId: 'probe-' + Date.now(), connectTimeout: 8000, reconnectPeriod: 0 });",
      "const r = crypto.randomUUID();",
      "",
      "c.on('connect', () => {",
      "  c.subscribe(subTopic('fs.read.response'), (err, granted) => {",
      "    if (err) { console.error('subscribe failed', err); process.exit(1); }",
      "    c.publish(buildTopic('fs.read.request'), JSON.stringify(env('fs.read.request', {",
      "      project_id: '<UUID_DEL_PROYECTO>',",
      "      path: '/recetas.json',",
      "      request_id: r",
      "    })));",
      "  });",
      "});",
      "",
      "c.on('message', (t, m) => {",
      "  try {",
      "    const d = JSON.parse(m.toString()).data || {};",
      "    if (d.request_id !== r) return;  // no es nuestra response",
      "    console.log('RESPONSE:', JSON.stringify(d).slice(0, 500));",
      "    process.exit(0);",
      "  } catch (e) { /* ignorar mensajes no-JSON */ }",
      "});",
      "",
      "setTimeout(() => { console.error('TIMEOUT'); process.exit(2); }, 15000);"
    ]
  },

  "relacion_con_otros_contratos": {
    "manual-audit-module-y-helpers": "Hermano. Cubre la skill audit-module v7 y los helpers atomicos en scripts/audit-helpers/. Cuando los helpers bastan, ese contrato es suficiente. Cuando hay que bajar a MQTT directo, este contrato continua la conversacion.",
    "events.contract": "Define la disciplina general de events del sistema (correlation_id, no_silent_failures, error.code canonico). El cliente directo DEBE respetar las garantias (no inyectar errores como texto en eventos de exito, etc.). No las puede romper publicando shapes mal formados.",
    "naming.contract / glossary.contract": "Los event_types siguen las reglas de naming.json (ASCII puro, kebab-case dentro de los segmentos, verbos canonicos del idioma del modulo). No inventar event_types nuevos desde un cliente ad-hoc — usar solo los canonicos del catalogo.",
    "security.contract": "Define la disciplina del par credential.resolve.request/response. NUNCA publicar credenciales desde un cliente ad-hoc al bus. NUNCA pasar API keys como parameters de tools.",
    "http.contract": "Para el endpoint HTTP de conversation-export (que NO va por MQTT). El bearer token y el shape del export estan ahi.",
    "llm-runtime-discipline.contract": "Las 10 reglas que el LLM-runtime sigue al ejecutar blueprints. Un cliente ad-hoc no es un LLM-runtime, pero si lo usas para emular un actor del sistema, respeta marcador_de_fuente_por_dato (source.core_id debe identificar quien eres) y confirmacion_antes_de_mutar_destructivo (no mutar produccion sin checks)."
  },

  "supersedes_nota": "v1.0.0 inicial — formaliza la conexion MQTT directa al bus de Enki como complemento al manual de audit-module/helpers. Cubre datos de conexion, topics de las dos familias (eventbus canonica + ui directa), envelope canonico, correlacion request/response, secuencias canonicas (listar proyectos, crear conv, enviar mensaje, par request/response generico), 5 probes de diagnostico forense aplicados en audits reales (audit recetas 2026-05-20 fue el caso testigo), catalogo de eventos seguros vs destructivos para clientes externos, errores clasicos al construir clientes ad-hoc, e implementacion de referencia minima pegable. Sin validator — es manual operativo, no contrato con enforcement mecanico."
}
