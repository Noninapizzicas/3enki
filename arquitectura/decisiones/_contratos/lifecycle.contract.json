{
  "_doc": "Captura LA forma canónica en la que un módulo nace y muere dentro del core. Cuándo se carga, qué recibe del core, dónde guarda referencias, qué timers/listeners reserva, cómo los suelta cuando se descarga. Es la 'ignición' y el 'apagado' del módulo. Sin esto, el core no sabe instanciarlo y el módulo no sabe limpiarse — y los hot-reloads se vuelven una pesadilla de fugas. Este contrato es la pareja del de events: un módulo nace, recibe el bus, publica/se suscribe, vive, y se apaga limpio.",
  "id": "lifecycle",
  "version": "1.0.0",
  "creada": "2026-04-30",
  "supersedes_nota": "Primera versión. Recoge el espíritu del Module Loader de event-core (autodescubrimiento + hot-reload + onLoad/onUnload) y formaliza las decisiones que en las evoluciones (Enki, Enki1, 2enki) se tomaron de forma ad-hoc por cada módulo.",

  "objetivo": "Establecer la 'ignición y apagado' canónico de un módulo. Producir UN JSON output (_outputs/lifecycle.json) con: forma del onLoad y onUnload, qué inyecta el core en cada módulo, política de captura de referencias, política de cleanup obligatorio, comportamiento esperado en hot-reload, máquina de estados del módulo, política de errores en arranque/apagado. Validable mecánicamente cruzando contra las auditorías modulo-completo de los 66 módulos.",

  "inputs": {
    "obligatorios": [
      "arquitectura/decisiones/_contratos/lifecycle.contract.json (este archivo)",
      "arquitectura/decisiones/_schemas/lifecycle.schema.json",
      "arquitectura/decisiones/_outputs/events.json (porque el bus es lo que el core inyecta)"
    ],
    "obligatorios_si_existen": [],
    "opcionales_para_contexto": [
      "arquitectura/auditoria/_outputs/modulo-completo/*.json — campo `lifecycle` de cada módulo (onLoad.acciones, onLoad.guarda_referencias_externas, onUnload.acciones, onUnload.estado_no_limpiado, subscribes_dinamicos_en_lifecycle)",
      "arquitectura/auditoria/_outputs/manifest-completo/*.json — declaraciones de eventos/tools/handlers que el module loader usa para auto-wiring",
      "modules/*/module.json — language declarado y declaraciones del module loader"
    ],
    "prohibido_leer": []
  },

  "filosofia": [
    "EL MÓDULO ES UN INVITADO. El core es el anfitrión. Cuando el core te invita (te llama a onLoad), te entrega lo que necesitas para vivir en su casa: el bus para hablar con los demás, el logger para contar lo tuyo, las métricas para que se te observe. Tú aceptas lo que recibes y lo guardas en this.* — no rebuscas en sus cajones.",

    "DECLARA, NO PIDAS. Lo que un módulo necesita de eventos lo declara en module.json (events.subscribes, tools, ui_handlers, apis). El core lee la declaración y hace el cableado. El módulo NO va por libre montando subscripciones en su onLoad — salvo casos justificados que se documentan.",

    "ONLOAD NO ES SUPLICA. onLoad NO espera respuestas de otros módulos. NO publica un evento y se queda escuchando una respuesta. NO hace requests sincrónicos a peers. Si necesitas algo de otro módulo en arranque, pide referencias al core y úsalas, o suscríbete al evento que indica que el otro está listo y reacciona cuando aparezca.",

    "LO QUE SE RESERVA SE LIBERA. Si en onLoad reservaste algo (un timer, un listener MQTT directo, un subproceso, un file watcher, un buffer grande, una conexión externa), en onUnload lo sueltas. La auditoría es la única verdad: si reservaste y no liberaste, queda como leak_potencial.",

    "EL ESTADO ES TUYO. El estado interno del módulo (this.X = new Map(), this.config, this.activeBuilds) es del módulo. El core no lo conoce ni le importa. Cuando el módulo se descarga, el estado se va con él. Hot-reload = estado fresco. Si necesitas persistir, usa el bus (emite el evento) o filesystem.",

    "HOT-RELOAD ES CIUDADANO DE PRIMERA. El módulo debe poder cargarse, descargarse, recargarse en runtime sin contaminar al sistema. Eso significa: cero estado global, cero singletons, cero referencias a versión vieja del propio módulo. Cada onLoad es como si fuera la primera vez.",

    "ARRANQUE NO BLOQUEANTE. onLoad no bloquea el loader. Si tienes que cargar 500MB de disco, lo haces async. Si tienes que conectar a un broker externo lento, lo haces async. El loader carga muchos módulos en paralelo; ningún módulo puede secuestrar el arranque del sistema.",

    "ERRORES VISIBLES. Si onLoad falla, el módulo lo registra (logger) y propaga al core (throw). El core decide qué hacer (deshabilitar el módulo, reintentar, marcar degradado). El módulo NO traga errores en silencio. Pero TAMPOCO se cae el sistema entero porque un módulo no arrancó.",

    "FLUJO PREDECIBLE. La máquina de estados del módulo es: unloaded → loading → loaded → unloading → unloaded. Sin atajos, sin estados intermedios inventados. El core llama onLoad, espera la promesa, marca loaded. Llama onUnload, espera la promesa, marca unloaded. Lineal."
  ],

  "principios": [
    {
      "id": "canonical_signatures",
      "regla": "Hay dos métodos canónicos del lifecycle: 'async onLoad(core)' y 'async onUnload()'. Ningún otro nombre, ninguna otra firma. onLoad recibe SIEMPRE un único parámetro: el core inyectado. onUnload no recibe parámetros.",
      "razon": "Una firma única hace el module loader trivial: lee la clase, llama a estos dos métodos. Cualquier variación (init/teardown/start/stop/setup) requiere lógica especial en el loader y rompe la uniformidad. No hay justificación para inventar nombres distintos."
    },
    {
      "id": "guest_relationship",
      "regla": "El módulo recibe del core solo lo que el core le inyecta. NO accede a propiedades internas del core, NO instancia clases del core por su cuenta, NO importa módulos del core directamente.",
      "razon": "Si el módulo accede al interior del core, queda acoplado a su implementación. Si el core cambia internamente, todos los módulos se rompen. La única superficie permitida es lo que el core inyecta como parámetro de onLoad."
    },
    {
      "id": "core_injection_minimal",
      "regla": "El core inyecta como mínimo: logger, metrics, eventBus. Opcionalmente: uiHandler, config, modules (registry). NO inyecta clases, instancias de servicios concretos, ni handles a otros módulos.",
      "razon": "Inyección mínima reduce el acoplamiento al core. Si el módulo necesita más cosas, las pide via eventBus (publica un evento, alguien le responde). El módulo no debería poder llamar 'core.databaseManager' — eso lo acopla al esquema interno del core."
    },
    {
      "id": "declarative_wiring",
      "regla": "Las suscripciones a eventos, tools del LLM, ui_handlers y APIs HTTP se DECLARAN en module.json. El module loader hace el wiring leyendo el manifest. El módulo NO llama a eventBus.subscribe() en onLoad salvo casos justificados (suscripciones dinámicas dependientes de runtime).",
      "razon": "Declarar en manifest hace el módulo introspectable: la auditoría sabe qué eventos consume sin leer el código. Los subscribes manuales en onLoad son drift que esconden información. Las excepciones (subscribes dinámicos) deben documentarse explícitamente en lifecycle.subscribes_dinamicos_en_lifecycle de la auditoría."
    },
    {
      "id": "state_local_to_module",
      "regla": "Todo el estado del módulo vive en this.* (propiedades de la instancia). NO hay variables módulo-nivel (let/const fuera de la clase) que guarden estado mutable. NO hay singletons globales propios del módulo. NO hay referencias a otras instancias del propio módulo.",
      "razon": "Las variables módulo-nivel no se limpian en hot-reload — quedan en memoria de la versión vieja del archivo. Los singletons globales son fugas de estado entre versiones. Solo las propiedades de this.* desaparecen cuando la instancia se descarta. Esto es lo que permite hot-reload limpio."
    },
    {
      "id": "symmetric_unload",
      "regla": "Todo lo que onLoad reserva (timers, listeners de proceso, subscripciones MQTT directas, file watchers, conexiones, subprocesos), onUnload lo libera. Auditoría: lo que aparece en lifecycle.onLoad.acciones como reserva debe aparecer en lifecycle.onUnload.acciones como liberación.",
      "razon": "Sin esta simetría, hot-reload acumula leaks: cada recarga deja un timer activo, un listener huérfano, un subproceso zombie. Después de N recargas el sistema se ralentiza o muere. La simetría es la única defensa robusta."
    },
    {
      "id": "non_blocking_load",
      "regla": "onLoad no bloquea el loader. Si necesita IO pesado (cargar disco grande, conectar a broker externo, escanear filesystem), lo hace async. El loader puede cargar muchos módulos en paralelo. onLoad NO debe esperar a otros módulos (ni publish-and-wait, ni pollings, ni timeouts artificiales).",
      "razon": "Si un módulo bloquea su propio onLoad esperando algo, el loader puede bloquearse o serializar accidentalmente la carga del sistema. Si necesita coordinar con otro módulo, suscríbete al evento que indica que el otro está listo (idealmente declarativo) y reacciona cuando aparezca."
    },
    {
      "id": "errors_propagate_to_core",
      "regla": "Si onLoad falla, el módulo loguea el error con this.logger.error y propaga (throw) al core. El core decide qué hacer (deshabilitar el módulo, reintentar, marcar degradado). Igualmente onUnload. Lo que NO está permitido: traga silencioso de errores con .catch(() => {}) o try/catch sin log.",
      "razon": "Si un módulo se cae arrancando y nadie se entera, el sistema queda en estado inconsistente: el module loader cree que arrancó, pero no responde a sus eventos declarados. Los errores visibles permiten al core (o al operador) tomar decisión. La doctrina coincide con el principio no_silent_catch del contrato events."
    },
    {
      "id": "hot_reload_safety",
      "regla": "El módulo debe ser hot-reload safe: cada onLoad produce estado fresco, cada onUnload libera limpio. No hay caches estáticos en module-scope, no hay referencias a la versión vieja del propio módulo, no hay coordinación con instancias previas de sí mismo.",
      "razon": "Hot-reload es ciudadano de primera (Module Loader del framework lo soporta nativamente). Si los módulos no son hot-reload-safe, perdemos esa capacidad y obligamos a reiniciar el core para cada cambio — pérdida de DX y de capacidad de evolución en runtime."
    },
    {
      "id": "predictable_state_machine",
      "regla": "El ciclo de vida tiene 5 estados: unloaded → loading → loaded → unloading → unloaded. El core lleva la cuenta. El módulo NO inventa estados intermedios propios ('initializing', 'ready', 'started') visibles al core — esos pueden ser estado interno del módulo (this.*) pero no parte del contrato con el loader.",
      "razon": "Una máquina de estados simple y conocida hace el sistema observable: el core sabe en cada momento qué módulos están loaded y cuáles unloaded. Si cada módulo invent ase su propio ciclo, el loader no podría tomar decisiones consistentes (cuándo enrutar mensajes, cuándo descargar, etc.)."
    }
  ],

  "decisiones_arquitectonicas": [
    {
      "id": "signature_onLoad",
      "decision": "Firma canónica: 'async onLoad(core)'. Parámetro único, async (devuelve Promise). El loader hace 'await module.onLoad(core)' y solo después marca el módulo como loaded. Si la promesa rechaza, marca failed.",
      "derivado_de": ["canonical_signatures", "non_blocking_load", "errors_propagate_to_core"],
      "razon": "Async permite IO no bloqueante. Una sola firma elimina la lógica especial en el loader. Que el loader haga await garantiza que cuando un módulo está marcado loaded, su estado interno está realmente listo (no a medias).",
      "notas": "Variantes prohibidas: init(), start(), setup(), constructor(deps), onLoad sin parámetros, onLoad sincrónico."
    },
    {
      "id": "signature_onUnload",
      "decision": "Firma canónica: 'async onUnload()'. Sin parámetros. Async. El loader hace 'await module.onUnload()' antes de marcar unloaded. Si rechaza o se cuelga (>5s timeout), el loader fuerza el unload y registra el problema.",
      "derivado_de": ["canonical_signatures", "symmetric_unload", "errors_propagate_to_core"],
      "razon": "El loader necesita garantía de que cuando marca unloaded, los recursos están liberados. El timeout protege contra módulos que se cuelgan en cleanup — el sistema no puede quedar bloqueado por un mal módulo.",
      "notas": "Si onUnload no existe en el módulo, el loader asume nada que limpiar. Si existe pero está vacío y onLoad reservó cosas, es leak — la auditoría lo marca."
    },
    {
      "id": "core_api_canonica",
      "decision": "El objeto 'core' inyectado en onLoad expone exactamente: logger (siempre), metrics (siempre), eventBus (siempre), uiHandler (opcional, presente si hay UI), config (opcional, namespaced por módulo), modules (opcional, registry de otros módulos para inspector-like cases). NO expone: clientes DB directos, cliente MQTT crudo, instancias de servicios concretos, handles a otros módulos.",
      "derivado_de": ["guest_relationship", "core_injection_minimal"],
      "razon": "Esta API mínima es la única superficie de contacto módulo↔core. Si un módulo necesita más (DB, otro módulo), va via eventBus.publish — y otro módulo lo resuelve. Mantener la API mínima evita que los módulos se acoplen al esquema interno del core.",
      "notas": "core.config se accede como core.config?.[this.name] — namespaced por nombre del módulo. NUNCA core.config['otro-modulo']."
    },
    {
      "id": "config_namespacing",
      "decision": "Cada módulo lee su configuración SOLO de 'core.config?.[this.name]'. El módulo merge esa slice con sus defaults: 'this.config = { ...defaults, ...core.config?.[this.name] }'. NO accede a config de otros módulos. NO modifica core.config en runtime.",
      "derivado_de": ["guest_relationship", "state_local_to_module"],
      "razon": "Namespacing por nombre evita choques entre módulos distintos. Un módulo no debe saber qué configuración usa otro — ni siquiera leerla. Si necesita coordinar parámetros, va via eventBus."
    },
    {
      "id": "module_state_machine",
      "decision": "5 estados llevados por el core (no por el módulo): unloaded (inicial) → loading (durante onLoad) → loaded (tras await onLoad y auto-wiring) → unloading (durante onUnload) → unloaded (tras await onUnload). Estados de fallo: failed (loading rechazado), unloaded_with_errors (onUnload rechazado/timeout).",
      "derivado_de": ["predictable_state_machine"],
      "razon": "Estados simples y enumerables permiten al core tomar decisiones consistentes (cuándo rutear eventos, cuándo descargar, cuándo reportar drift). Estados privados del módulo (initializing, warming-up) son estado interno (this.*) y no parte del contrato con el loader.",
      "notas": "Transiciones disparadas por el core, no por el módulo. El módulo NO publica eventos del tipo 'module.X.ready' por su cuenta — el core lo hace tras marcar loaded."
    },
    {
      "id": "auto_wiring_orden",
      "decision": "Secuencia que ejecuta el module loader entre unloaded y loaded: (1) leer module.json, (2) instanciar la clase del módulo, (3) marcar loading, (4) await onLoad(core), (5) auto-wire subscribes de manifest.events.subscribes, (6) auto-wire tools, (7) auto-wire ui_handlers, (8) auto-wire HTTP routes, (9) marcar loaded, (10) publicar status retained con el módulo añadido.",
      "derivado_de": ["declarative_wiring", "predictable_state_machine"],
      "razon": "onLoad va ANTES del auto-wiring para que cuando lleguen los primeros eventos, el módulo tenga el estado listo (this.config cargado, this.X = new Map() inicializado, conexiones abiertas). Si el wiring fuera primero, podríamos recibir eventos en una instancia a medio inicializar.",
      "notas": "Si onLoad reserva timers/listeners propios (no auto-wired), eso ocurre dentro del onLoad — paso 4. El paso 5+ es solo para lo declarado en manifest."
    },
    {
      "id": "hot_reload_protocol",
      "decision": "Secuencia de hot-reload disparada por fs.watch: (1) marcar unloading, (2) auto-unwire subscribes/tools/handlers, (3) await onUnload(), (4) descartar instancia y referencia a la clase, (5) clear require.cache para el archivo, (6) re-require fresh, (7) instanciar la nueva clase, (8) marcar loading, (9) await onLoad(core), (10) auto-wire, (11) marcar loaded, (12) publicar status update.",
      "derivado_de": ["hot_reload_safety", "symmetric_unload"],
      "razon": "El protocolo es estricto para garantizar que NO queden referencias a la versión vieja: descartar instancia + clear require.cache + re-require. Si el módulo tiene state module-scope (lo prohibido), aquí se rompe — esa es la auditoría natural del principio state_local_to_module.",
      "notas": "El estado de la instancia anterior se PIERDE en hot-reload (es deliberado). Si se necesita persistir entre versiones, se persiste a disco/bus en onUnload y se recarga en onLoad."
    },
    {
      "id": "error_handling_arranque",
      "decision": "Si onLoad rechaza (throw o Promise reject): (1) el loader captura el error, (2) loguea con stack trace completo, (3) marca el módulo como failed (estado terminal), (4) publica 'core/{core-id}/module/{name}/load_failed' con detalles, (5) NO auto-wire (handlers no existen, eventos a este módulo se pierden o causan warning), (6) continúa cargando otros módulos. Operador puede reintentar manualmente.",
      "derivado_de": ["errors_propagate_to_core", "non_blocking_load"],
      "razon": "Un módulo que falla en arranque NO puede romper el sistema entero — los demás módulos siguen su vida. Pero el fallo es visible (log + evento de status) para que el operador o un módulo de health monitoring lo detecte. El módulo failed queda inactivo hasta retry manual.",
      "notas": "Diferente de 'unloaded': failed indica intento fallido, unloaded indica nunca cargado o descargado limpiamente."
    },
    {
      "id": "error_handling_apagado",
      "decision": "Si onUnload rechaza o no resuelve en 5s (timeout): (1) loguear el error/timeout, (2) marcar el módulo como unloaded_with_errors, (3) forzar liberación de referencias del módulo (let GC collect), (4) publicar 'core/{core-id}/module/{name}/unload_error' con detalles, (5) los recursos NO liberados (timers, listeners, subprocesos) quedan como leaks aceptados — el operador decide reiniciar el core si el problema se repite.",
      "derivado_de": ["symmetric_unload", "errors_propagate_to_core"],
      "razon": "El sistema no puede quedar bloqueado por un módulo malo en cleanup. El timeout 5s es generoso (suficiente para flush de buffers, close de conexiones razonables). Pasado eso, mejor un leak controlado y visible que un sistema colgado.",
      "notas": "Los leaks reportados deberían bajar drásticamente si los módulos respetan symmetric_unload. Si un módulo aparece repetidamente con unload_error, es candidato prioritario a refactor."
    },
    {
      "id": "subscribes_dinamicos_documentacion",
      "decision": "Las suscripciones dinámicas (registradas en runtime en lugar de declarativamente en module.json) están permitidas SOLO si: (a) dependen de valores runtime (project_id, device_id, topic dinámico MQTT), Y (b) están explícitamente documentadas. Documentación: en module.json bajo events.subscribes_dynamic[] (con descripción de la condición), o en la auditoría modulo-completo en lifecycle.subscribes_dinamicos_en_lifecycle. La auditoría detecta drift: subscribes presentes en código pero no en ninguna de esas dos vías.",
      "derivado_de": ["declarative_wiring", "symmetric_unload"],
      "razon": "Las suscripciones dinámicas son válidas pero opacas — el module loader no puede auto-wire algo que aparece en mitad de onLoad. Documentarlas las hace introspectables. La cancelación en onUnload sigue siendo obligatoria (symmetric_unload), porque el loader no puede unwire automáticamente lo que no auto-wireó."
    }
  ]
}
